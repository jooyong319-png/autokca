# 오또케 — 개선 작업 문서 (1차 검증)

> `autokca.com` / 검증 시점: 2026-08-13
> 검증 범위: 홈, 질문 상세(`/q/축의금-5만원`), 카카오톡 공유 미리보기, 서버 렌더링 HTML
> 미검증: 모바일 레이아웃, 투표 전 화면 상태

---

## 0. 현재 평가 요약

컨셉과 실행이 잘 맞물려 있습니다. 유지해야 할 것과 고쳐야 할 것을 먼저 구분합니다.

### 유지할 것 (건드리지 말 것)

- **투표용지/공문서 컨셉의 시각화** — 크림색 종이 카드 + 짙은 갈색 배경, 테두리 선택지 표, 빨간 도장
- **"제001-8255호 안건 / 별건"** 문서번호 체계
- **사극 말투 선택지** — "적다오 / 괜찮다오", "어떻게든 안 켠다오"
- **도장 인터랙션** — 이 사이트만의 고유 요소. 캡처했을 때 식별성이 여기서 나옴
- **댓글 섹션 "왜 그런지"** — 진영 분리("적다 쪽으로 올라갑니다") 구현됨
- **댓글 안내 카피** — "링크와 연락처는 막습니다. 광고는 티가 납니다."
- **하단 면책 문구** — "여론조사가 아닙니다"
- **약관 / 개인정보처리방침 / 연락처** — 애드센스 심사 요건

유머를 질문 소재가 아니라 **인터페이스 자체**에 넣는 방향이 잘 실행되었습니다.
소재가 고갈되어도 톤이 유지되는 구조이므로 이 방향을 계속 밀어야 합니다.

---

## 1. 최우선 수정 (P0)

### 1-1. 한국어 줄바꿈 깨짐

**현상**

```
친구 결혼식 축의금 5
만원이면 적나요?
```

"5"와 "만원"이 분리되어 줄바꿈됨. 영문 기준 줄바꿈 규칙이 적용된 상태.

**수정**

전역 CSS에 적용:

```css
:root, body {
  word-break: keep-all;
  overflow-wrap: break-word;
}
```

- `keep-all`은 어절 단위로 줄바꿈 → "축의금 / 5만원이면 적나요?"
- **질문 제목만이 아니라 사이트 전체 텍스트에 적용** (댓글, 카피, 결과 문구 포함)
- 숫자+단위는 추가 보호: `<span class="nowrap">5만원</span>` (`white-space: nowrap`)
- OG 이미지 생성 로직(`opengraph-image`)에도 동일 규칙 적용 필요

> 비용 대비 효과가 가장 큰 항목. 최우선 처리.

---

### 1-2. 표본이 적을 때 퍼센트를 노출하지 말 것

**현상**

```
응답자 1명 중 100%가 "적다"를 골랐습니다
무난하네요. 재미없어요. (100%)
```

1표만 있는 상태에서 100% 및 판정 카피가 출력됨.
의도한 유머가 아니라 **버그로 인식됨**. 카카오톡 공유 카드에도 그대로 노출되어
"응답자 1명 중 100%"가 미리보기에 표시됨 → 죽은 사이트로 보임.

**수정 — 표본 임계치 도입**

| 총 투표 수 | 노출 내용 |
|---|---|
| 0 | "아직 개표 전입니다" (현재 "첫 표를 던져 보세요" 유지 가능) |
| 1 ~ 19 | 퍼센트·막대그래프 **숨김**. 총 투표 수만 표시 |
| 20 ~ 49 | 퍼센트·막대 공개. 판정 카피는 숨김 |
| 50 이상 | 전체 공개 (퍼센트 + 판정 카피) |

**적용 범위 — 아래 4곳 전부에 동일 규칙**

1. 질문 상세 페이지
2. 홈 피드 카드
3. **"결과 복사" 텍스트**
4. **OG 이미지** (`opengraph-image`)

**대체 카피 예시** (공문서 톤 유지)

- 0표: "아직 아무도 안 눌렀습니다. 첫 표를 던져 보세요." (현행 유지)
- 1~19표: "개표 중입니다. 아직 3표라 발표할 수 없습니다."
- 20~49표: 퍼센트만, 판정 없음

> 이 로직 하나로 콜드스타트 문제가 디자인 차원에서 해결됨.
> 0표가 "부끄러운 상태"가 아니라 "개표 전"이라는 정상 상태가 됨.

---

### 1-3. 투표 전 결과 노출 여부 확인

**확인 필요 사항**: 투표하기 전에 막대그래프/퍼센트가 보이는가?

보인다면 반드시 수정:

- 결과를 먼저 보면 **투표할 동기가 사라짐** (궁금증이 이미 해소됨)
- **밴드왜건 효과**로 다수 쪽에 표가 쏠려 데이터가 왜곡됨

**올바른 동작**

1. 투표 전: 질문 + 선택지만 표시
2. 도장 클릭 → 도장 찍히는 애니메이션
3. 결과가 나타남 (막대 애니메이션)
4. 댓글 입력창 활성화

---

## 2. 전환·PV 관련 (P1)

### 2-1. 상세 페이지의 "이 질문만 보기 →" 제거

`/q/축의금-5만원` 페이지에도 "이 질문만 보기 →" 버튼이 노출됨. 이미 해당 페이지이므로 무의미.

**수정**: 상세 페이지에서는 해당 버튼을 숨기고 **"다음 질문 →"** 으로 교체.

세션당 PV가 매출을 결정하는 구조이므로, 상세 페이지에서 사용자를 다음 질문으로
넘기는 동선이 반드시 필요합니다. 현재는 이 자리가 비어 있어 이탈 지점이 됩니다.

추가 검토: 상세 페이지 하단에도 홈과 동일한 무한 스크롤 피드를 붙이는 방안.
(검색 유입은 대부분 상세 페이지로 착지하므로, 여기서 이어지지 않으면 1PV로 끝남)

### 2-2. 공유 텍스트에 후킹 문구 추가

현재 복사 텍스트는 결과 수치 위주. 아직 투표하지 않은 사람이 클릭할 이유가 약함.

**수정**: 결과 아래 한 줄 추가 — "당신은 어느 쪽?" 형태의 참여 유도 문구.
단, 표본 임계치 미만이면 결과 수치는 빼고 질문 + 후킹 문구만 노출.

---

## 3. UI 개선 (P2)

### 3-1. 선택지의 클릭 가능성이 전달되지 않음

현재 "적다 / 괜찮다"가 표 안의 정적 텍스트처럼 보임.
오른쪽 빈 칸이 도장 자리라는 것을 첫 방문자는 알 수 없음.

**수정안**

- 표 상단에 작은 안내: "도장을 찍어 주세요"
- **hover / tap 시 도장이 반투명(opacity 0.25~0.35)으로 미리 표시** ← 이것만으로 대부분 해결됨
- `cursor: pointer`
- 터치 영역은 행 전체(선택지 텍스트 칸 포함)

### 3-2. 데스크톱 여백 처리

카드 폭 약 500px, 화면 1900px 기준 좌우 배경이 화면의 약 70%를 차지.
모바일 우선 설계는 옳으나 데스크톱에서 허전함.

**수정안 (택1 또는 조합)**

- 배경에 옅은 **종이 텍스처 / 격자 무늬** (서류철 느낌, 컨셉과 일치)
- 카드 최대 폭을 620~680px로 소폭 확대
- 좌우에 도장·괘선 등 장식 요소를 낮은 투명도로 배치

**금지**: 사이드바나 추가 정보 영역을 넣지 말 것. 무한 스크롤의 리듬이 깨짐.

### 3-3. 태그라인 노출 위치

"이거 나만 그래?"가 우측 상단에 작은 회색 텍스트로 배치되어 있음.
사이트를 한 줄로 설명하는 문구인데 첫 방문자가 놓치기 쉬움.

**수정**: 로고 바로 아래 또는 첫 카드 상단에 더 큰 크기로 배치.

### 3-4. "안건" vs "별건" 구분 기준 안내

우측 상단에 표기되나 기준이 사용자에게 전달되지 않음.
진지/병맛 질문 구분이라면 `/about`에 한 줄 설명을 추가하면 장치로 작동함.
설명이 없으면 단순 노이즈로 인식됨.

---

## 4. SEO (P1)

### 4-1. 홈 메타 태그 분리 — 미확인, 재확인 필요

이전 검증 시점의 홈 HTML에서 아래 문제가 확인되었습니다. 수정되었는지 확인 필요.

```
title: 친구 결혼식 축의금 5만원이면 적나요?   ← 홈인데 첫 질문 제목
canonical: https://autokca.com
og:title: 친구 결혼식 축의금 5만원이면 적나요?
```

**문제**

1. 동일 콘텐츠가 홈과 `/q/축의금-5만원` 두 URL에 존재 → 중복 판정 위험
2. 브랜드 검색("오또케") 시 노출 제목이 질문 제목이 되어 사이트 정체 전달 실패

**수정**

- 홈 전용 메타: `title: 오또케 — 이거 나만 그래?`, description은 사이트 소개
- 홈 OG 이미지도 사이트 대표 이미지로 (질문별 OG는 `/q/slug`가 담당)
- 각 질문의 canonical은 `/q/slug`
- 홈 피드 카드는 제목 + 선택지까지만 서버 렌더링. 댓글 등 본문은 상세 페이지에만

### 4-2. thin content 대응

투표 수와 댓글이 적은 질문 페이지는 텍스트가 두세 줄에 불과 → 색인되어도 순위를 받지 못하고,
빈 페이지가 대량 색인되면 **사이트 전체 품질 평가가 하락**함.

**수정**

- 투표 20표 미만 **또는** 댓글 0개인 질문은 `noindex`
- 임계치 초과 시 자동으로 `noindex` 해제
- `sitemap.xml`에도 임계치 통과 페이지만 포함

### 4-3. 구조화 데이터

- 질문 페이지에 `DiscussionForumPosting` 적용
- 결과 수치를 **텍스트로도 노출** (현재 "응답자 N명 중 X%가 …를 골랐습니다" 형태로 이미 구현됨 — 유지)
- `BreadcrumbList` 적용

---

## 5. 운영 (개발 외 항목)

### 5-1. 론칭 전 실제 투표 채우기

현재 전 질문이 0~1표. **가짜 표는 절대 넣지 말 것** — "집계는 실제 투표 수"라고
명시했으므로 이를 위반하면 브랜드 신뢰가 무너집니다.

대신 지인·단톡방에 링크를 돌려 질문당 20~30표를 실제로 확보합니다. 하루면 충분하고,
표본 임계치(1-2)를 넘기면 사이트 인상이 완전히 달라집니다.

### 5-2. 피드 정렬

0표 질문이 첫 화면에 오지 않도록, 초기에는 **투표 수가 많은 순**으로 정렬.
표본이 충분히 쌓인 뒤 최신순/랜덤으로 전환.

### 5-3. 사이트 전체 누적 수치 노출

헤더 또는 푸터에 "지금까지 N표" 표시. 개별 질문의 표가 적어도 사이트 전체는
활성 상태로 보이게 하는 장치.

---

## 6. 작업 우선순위

| 순위 | 항목 | 위치 | 비고 |
|---|---|---|---|
| 1 | `word-break: keep-all` 전역 적용 | 1-1 | 비용 최소, 효과 큼 |
| 2 | 표본 임계치 (%·판정·복사·OG 전부) | 1-2 | 콜드스타트 문제 해결 |
| 3 | 투표 전 결과 노출 여부 확인·차단 | 1-3 | 참여 동기 직결 |
| 4 | 홈 메타 태그 분리 | 4-1 | 색인 누적 전에 처리 |
| 5 | 상세 페이지 "다음 질문 →" | 2-1 | PV 직결 |
| 6 | thin content `noindex` 로직 | 4-2 | 대량 색인 전에 처리 |
| 7 | 선택지 클릭 affordance (도장 프리뷰) | 3-1 | 첫 방문자 이해도 |
| 8 | 공유 텍스트 후킹 문구 | 2-2 | |
| 9 | 태그라인 위치, 안건/별건 설명 | 3-3, 3-4 | |
| 10 | 데스크톱 배경·여백 | 3-2 | 급하지 않음 |

---

## 7. 미검증 — 추가 확인 필요

- **모바일 레이아웃**: 실제 트래픽 대부분이 모바일(커뮤니티 유입)이므로 데스크톱보다 중요.
  한 화면에 질문 하나가 온전히 들어오는지 확인 필요 (무한 스크롤의 리듬)
- **투표 전 화면 상태** (1-3)
- **댓글 작성 흐름**: 투표 전 입력 시도 시 어떻게 처리되는지
- **반대 진영 최고 추천 댓글 노출** 여부 (기획상 상단 고정 예정이었음)
- 페이지 로드 성능 (LCP, INP)

---

## 8. 진행 상태 (2026-08-13 · 작업 중단 지점)

배포된 사이트를 실제로 확인해서 문서의 "미확인" 항목을 가려냈다. **아직 코드는 하나도 안 고쳤다.**

### 이미 되어 있던 것 — 문서에서 지워도 된다

| 항목 | 확인 결과 |
|---|---|
| 2-1 "상세 페이지에도 무한 스크롤 피드" (추가 검토) | ✅ 이미 붙어 있다 (카드 3개 이어짐) |
| 4-3 `DiscussionForumPosting` · `BreadcrumbList` | ✅ 둘 다 적용됨 |
| 4-3 결과 수치 텍스트 노출 | ✅ "응답자 N명 중 X%가 …" 구현됨 |
| 7 진영 2열 분리 | ✅ 2열 렌더됨 |
| 7 반대 진영 최고 추천 상단 고정 | ✅ 구현됨. 지금은 **댓글이 0개라 표시 조건 미충족**일 뿐 |
| 7 댓글 작성 흐름 | ✅ 투표 전 작성 시도 → `403 "투표부터 하세요."` |

### 실제로 안 되어 있는 것 — 확인된 현상

| 항목 | 실측 |
|---|---|
| **1-3** 투표 전 결과 노출 | 🔴 홈에서 막대 6개(카드 3장 × 2) 노출 중. 투표 안 해도 다 보인다 |
| **1-2** 표본 임계치 | 🔴 `응답자 1명 중 100%가 "적다"를 골랐습니다` — **메타 설명에도 박혀 카톡 미리보기에 그대로 나간다** |
| **4-1** 홈 메타 | 🔴 홈 `<title>`이 `친구 결혼식 축의금 5만원이면 적나요? — 100%가 "적다"`. og:title도 동일 |
| **2-1** "이 질문만 보기 →" | 🔴 상세 페이지에도 렌더됨 (자기 자신을 가리킴) |
| **1-1** 한국어 줄바꿈 | 🔴 `word-break` 미적용 |

### 다음에 이어서 할 일 — 문서 §6 우선순위대로

1. `word-break: keep-all` 전역 (`app/globals.css`) + OG 카드에도 동일 규칙
2. **표본 임계치**를 `lib/`에 단일 소스로 만들고 4곳에 물린다 —
   `components/Ballot.tsx`(화면·복사) · `lib/seo.ts`(title·description) · `lib/ogCard.tsx`(OG)
   - 0표 / 1~19 / 20~49 / 50+ 4단계
   - ⚠️ 지금 `lib/site.ts`의 `TURNOUT_THRESHOLD = 100`과 겹친다. 임계치 체계를 하나로 합쳐야 한다
3. 투표 전 막대·수치 숨김 (`Ballot`의 `hasResult` 조건에 "내가 투표했는가"를 넣는다)
   - ⚠️ 브리프 §6("수치를 텍스트로 노출")과 부딪히는 지점이다.
     **해소 방향**: 화면은 투표 후 공개, 수치는 title·description·OG가 담당(스니펫용).
     본문 두께는 댓글이 채운다(4-2가 thin이면 noindex라 문제없음)
4. 홈 전용 메타 + 홈 OG를 사이트 대표 카드로 (질문별 OG는 `/q/[slug]`가 담당)
5. 상세에서 "이 질문만 보기" → "다음 질문 →"
6. thin content 임계치 재조정 — 문서는 **20표 미만 또는 댓글 0개**. 현재는 30표 **및** 댓글 3.
   사이트맵도 통과한 페이지만 넣어야 한다 → `all_comment_counts()` RPC가 필요하다
7. 도장 hover 프리뷰(opacity 0.25~0.35)
8. 공유 텍스트 후킹 문구
9. 태그라인 위치 · `/about`에 안건/별건 설명
10. 데스크톱 배경 텍스처
11. 운영: 피드 정렬(표 많은 순) · 누적 표 수 노출(`/api/stats` + 클라이언트 표시로 정적 렌더 유지)

### 아직 확인 못 한 것

- **모바일 레이아웃** — 실기기로 본 적 없다. 커뮤니티 유입이 대부분 모바일이라 데스크톱보다 중요하다
- LCP·INP
- 접근성 감사(axe) — 딱칼크는 했는데 여기는 안 했다

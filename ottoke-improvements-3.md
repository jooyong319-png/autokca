# 오또케 — 개선 작업 문서 (3차)

> `autokca.com` / 작성 시점: 2026-08-13
> 2차 문서(`ottoke-improvements-2.md`)의 후속
> **핵심 신규 항목: 한 화면 = 한 질문 구조로 전환 (scroll-snap)**

---

## 0. 이 문서의 구성

| 장 | 내용 | 성격 |
|---|---|---|
| 1 | 2차 반영 현황 | 확인 |
| 2 | **scroll-snap 전환 설계** | **신규 · 핵심** |
| 3 | 댓글 위치 재설계 | 2장에 종속 |
| 4 | 광고 배치 재설계 | 2장에 종속 |
| 5 | 2차 잔여 항목 | 계속 진행 |
| 6 | 캐시 확인 | 확인 필요 |
| 7 | 우선순위 | |

---

## 1. 2차 문서 반영 현황

### 반영 확인 (건드리지 말 것)

- ✅ **홈 메타 태그 분리** — 브라우저 탭 제목 "오또케?! — 이거 나만 그래?" 확인
- ✅ **홈 데이터 갱신** — 홈에서도 실제 투표 수("1명 참여") 반영. stale 해소
- ✅ **도장 플레이스홀더** — 미투표 칸에 옅은 점선 원 + "오" 표시. 빈 칸 문제 해결됨
- ✅ **참여자 수 표시** — "1명 참여"
- ✅ **카드 폭 확대** — 약 590px → 약 680px
- ✅ **배경 격자 텍스처** — 좌우 여백에 옅은 격자 적용. 허전함 상당 부분 개선
- ✅ **태그라인 위치** — 로고 하단으로 이동, 붉은색으로 가시성 확보
- ✅ **투표 완료 카드는 "도장을 찍어 주세요" 안내 숨김** — 조건부 처리 정확

### 미반영 (계속 필요)

- ❌ **sticky 헤더** — 스크롤 시 헤더가 완전히 사라짐. 사이트 정체 인지 불가
- ❌ **댓글 수 표시** — 카드에 여전히 없음. 상세 페이지 유입 동기 부재
- ❌ **카드 내부 여백 축소** — 선택지 표 → 참여자 수 → 결과 문구 → 버튼 간 간격이 여전히 넓음
- ⏸ 인피드 광고 — 애드센스 승인 전이므로 현 시점 미착수 정상

---

## 2. 한 화면 = 한 질문 구조로 전환 (신규 · 핵심)

### 2-1. 목적

현재 무한 스크롤 피드는 데스크톱에서 카드 사이 여백과 배경이 크게 드러납니다.
카드를 화면 높이에 맞추면 **배경 여백 문제가 구조적으로 사라지고**, 서류를 한 장씩
넘기는 컨셉과도 일치합니다.

### 2-2. 스크롤을 차단하지 말 것 (중요)

`overflow: hidden` + 버튼 전용 이동 방식은 **채택하지 않습니다.**

- **모바일에서 치명적**: 스와이프가 먹지 않으면 사용자는 "사이트가 멈췄다"고 판단하고
  버튼을 찾기 전에 뒤로가기를 누릅니다. 트래픽 대부분이 커뮤니티 → 모바일이므로 위험이 큼
- 키보드 사용자, 스크린리더, 트랙패드 관성 스크롤이 모두 무력화됨 (접근성 훼손)
- 스크롤 하이재킹은 일반적으로 이탈률을 높이는 패턴

### 2-3. 채택 방식 — CSS scroll-snap

스크롤은 그대로 살리되, **카드 단위로 정확히 멈추게** 합니다. (틱톡·릴스 방식)

```css
.feed {
  height: 100dvh;
  overflow-y: scroll;
  scroll-snap-type: y mandatory;
  scroll-behavior: smooth;
}

.card-slide {
  height: 100dvh;
  scroll-snap-align: start;
  scroll-snap-stop: always;   /* 빠른 스와이프로 여러 장 건너뛰는 것 방지 */
  display: flex;
  align-items: center;
  justify-content: center;
}
```

**보조 이동 수단** (주 이동 수단이 아님)

- 데스크톱: 화면 우측에 **위/아래 버튼** 배치
- **키보드**: ↑ / ↓ / PageUp / PageDown / Space 지원
- 두 경우 모두 `scrollIntoView({ behavior: 'smooth' })` 로 처리

### 2-4. 모바일 뷰포트 주의

```css
.card-slide {
  height: 100vh;   /* 폴백 */
  height: 100dvh;  /* 실제 적용 */
}
```

모바일 브라우저 주소창 때문에 `100vh`는 실제 화면보다 큽니다. 반드시 `100dvh`를 사용하고
`100vh`를 폴백으로 함께 선언합니다.

### 2-5. 긴 질문 대응

질문이 길거나 선택지 텍스트가 길면 한 화면에 들어가지 않을 수 있습니다.

- `scroll-snap-align: start` 유지
- 카드 내부는 `overflow-y: auto` 로 열어 둘 것 (내용이 넘칠 때만 스크롤)
- 카드 최소 높이는 `100dvh`, 내용이 더 길면 자연스럽게 늘어나도록

### 2-6. 카드 레이아웃 조정

한 화면을 차지하게 되므로 카드 구성이 달라집니다.

- 카드를 화면 세로 중앙 정렬
- 카드 폭: 데스크톱 720~780px
- 하단에 **다음 카드 상단 5~8%가 살짝 걸치게** 배치 → 아래에 더 있다는 신호
- 카드 내부 여백은 2차 문서 6-3대로 축소 (한 화면에 담아야 하므로 더 중요해짐)

### 2-7. 진행 상태 표시

한 장씩 넘기는 구조에서는 현재 위치를 알 수 없으면 답답합니다.

- 화면 우측 또는 상단에 얇은 **진행 인디케이터**
- 또는 공문서 톤으로 "제3호 / 오늘 12건" 형태 표기
- sticky 헤더(5-1)와 같은 영역에 배치하면 자연스러움

### 2-8. 적용 범위

| 페이지 | scroll-snap | 비고 |
|---|---|---|
| 홈 (`/`) | **적용** | 피드 |
| 질문 상세 (`/q/slug`) | **미적용** | 댓글이 있어 자유 스크롤 필요 |
| `/q`, `/best`, `/me` | 미적용 | 목록·집계 페이지 |

---

## 3. 댓글 위치 재설계 (2장에 종속)

### 문제

댓글은 개수에 따라 길이가 가변적이라 **한 화면에 고정할 수 없습니다.**
댓글 20개인 질문과 0개인 질문을 같은 높이로 처리할 방법이 없습니다.

### 해결 — 피드는 질문만, 댓글은 상세로

- **피드 카드**: 질문 + 선택지 + 결과 + **댓글 수**만 표시
- **댓글 본문**: `/q/slug` 상세 페이지에서만 노출
- 상세 페이지는 scroll-snap 미적용, 자유 스크롤

### 부수 효과 (오히려 이득)

현재 카드에서 상세 페이지로 넘어갈 동기가 없다는 것이 문제였습니다.
댓글이 상세에만 존재하면 **"왜 그런지 12"를 누를 이유가 생깁니다.**

- 상세 페이지 체류 시간 증가
- 댓글이 SEO 본문이므로 상세 페이지 콘텐츠 두께 확보
- 상세 페이지가 색인 임계치를 넘기기 쉬워짐

### 카드에 표시할 댓글 수 형식

```
왜 그런지 12
```

- 0개일 때는 "왜 그런지 남기기" 등으로 표시하여 작성 유도
- 클릭 시 `/q/slug` 의 댓글 섹션으로 앵커 이동

---

## 4. 광고 배치 재설계 (2장에 종속)

2차 문서 3장에서 인피드 광고를 설계했으나, 한 화면 = 한 카드 구조에서는 방식이 달라집니다.

### 방식 A — 광고 전용 슬라이드 (권장하지 않음, 단독 사용 시)

- 질문 카드 3~4장마다 광고만 있는 슬라이드 1장
- **위험**: 전면이 광고면 이탈률이 크게 오름
- 사용한다면 **광고 슬라이드 하단에 다음 질문 카드 상단을 크게 걸쳐서**(20~25%)
  "다음이 있다"는 것을 반드시 보여줄 것

### 방식 B — 카드 하단 배너 (권장)

- 각 카드 하단, 버튼 아래에 배너 슬롯
- 3~4장마다 1개만 실제 노출 (나머지는 빈 슬롯)
- 화면 흐름을 끊지 않음

### 방식 C — A + B 혼합

- 기본은 B, 5~6장마다 한 번씩 A
- 실제 이탈률을 보며 비율 조정

**공통 주의**

- 애드센스는 화면 대비 광고 비율 정책이 있음. 전면 광고 슬라이드는 정책 위반 소지 확인 필요
- 광고 슬롯도 **종이 카드 스타일로 감싸서** 컨셉을 유지
- 무한 스크롤로 새 카드를 로드할 때 광고 슬롯도 함께 주입

---

## 5. 2차 문서 잔여 항목

### 5-1. sticky 헤더 (미반영, 계속 필요)

스크롤 시 헤더가 완전히 사라져 사이트 정체를 알 수 없습니다.
scroll-snap 구조에서는 더 중요해집니다 (한 장씩 넘기면 컨텍스트가 더 쉽게 사라짐).

- 로고만 포함한 얇은 sticky 헤더
- 2-7의 진행 인디케이터를 같은 영역에 배치
- 높이는 44~52px 이내로 최소화

### 5-2. 댓글 수 표시 (미반영)

3장에서 재설계했으므로 그쪽 기준으로 구현.

### 5-3. 카드 내부 여백 축소 (미반영)

선택지 표 → "1명 참여" → 결과 문구 → 버튼 사이 간격을 **20~30% 축소**.
scroll-snap 적용 시 한 화면에 담아야 하므로 필수 항목이 됩니다.

### 5-4. `/q`, `/best` index 허용 (미확인)

개별 질문이 `noindex`인 상태에서 목록 페이지까지 막히면 색인 경로가 없습니다.
목록·집계 페이지는 `index` 허용, sitemap에 포함.

---

## 6. 캐시 확인 필요 (중요)

### 현상

브라우저에서는 최신 버전이 보이나(탭 제목 변경 확인),
**외부에서 `https://autokca.com/` 을 요청하면 3회 연속 이전 버전 HTML이 반환**됩니다.
OG 이미지 해시(`bde1481818ad1f29`)까지 이전과 동일합니다.

### 확인 방법

```bash
curl -sI https://autokca.com/ | grep -i -E 'cache-control|age|x-vercel-cache|cf-cache-status'
curl -s https://autokca.com/ | grep -o '<title>[^<]*</title>'
```

- `x-vercel-cache: HIT` 이고 `age` 값이 크면 CDN 캐시가 갱신되지 않는 상태
- 반환된 `<title>` 이 질문 제목이면 크롤러도 이전 HTML을 보고 있는 것

### 문제가 되는 이유

**구글봇도 CDN을 거칩니다.** 캐시가 갱신되지 않으면 색인되는 내용이 옛 버전이 됩니다.
홈 메타 태그를 수정해도 검색엔진에는 반영되지 않습니다.

### 조치

- 배포 시 CDN 캐시가 무효화되는지 확인
- `Cache-Control` 의 `s-maxage` 값 점검
- Next.js ISR을 쓴다면 `revalidate` 값과 실제 재생성 여부 확인
- 필요 시 배포 후크에서 캐시 퍼지 실행

> 요청 측 캐시일 가능성도 있으므로, 위 `curl` 로 직접 확인한 뒤 판단할 것.

---

## 7. 작업 우선순위

| 순위 | 항목 | 위치 | 비고 |
|---|---|---|---|
| 1 | 캐시 상태 확인 | 6 | 확인만. 문제 있으면 최우선 |
| 2 | scroll-snap 피드 전환 | 2 | 이번 사이클 핵심 |
| 3 | 댓글을 상세 페이지로 분리 + 카드에 댓글 수 | 3 | 2번과 함께 진행 |
| 4 | 카드 내부 여백 축소 | 5-3 | 2번의 전제 조건 |
| 5 | sticky 헤더 + 진행 인디케이터 | 5-1, 2-7 | |
| 6 | `/q`, `/best` index 허용 | 5-4 | |
| 7 | 광고 슬롯 구조 설계 | 4 | 애드센스 승인 후 실제 배치 |

---

## 8. 유지할 것 (개선 작업 중 훼손 금지)

- 투표용지 / 공문서 컨셉 — 크림색 종이 카드, 테두리 선택지 표, 빨간 도장
- 도장 플레이스홀더(점선 원) — 2차에서 추가된 것. 유지
- "제001-8255호 안건 / 별건" 문서번호 체계
- 사극 말투 선택지 — "적다오 / 괜찮다오"
- 표본 임계치 로직 — 20표 미만 퍼센트 숨김, 50표 미만 판정 카피 숨김
- 참여자 수는 항상 노출
- 댓글 섹션 "왜 그런지" 및 진영 분리
- 댓글 안내 카피 — "링크와 연락처는 막습니다. 광고는 티가 납니다."
- 하단 면책 문구 — "여론조사가 아닙니다"
- 배경 격자 텍스처

---

## 9. 미검증 항목

- **투표 전 결과 노출 여부** — 투표 전에 퍼센트/막대가 보이면 안 됨
  (참여 동기 상실 + 밴드왜건으로 데이터 왜곡). 계속 미확인 상태
- 반대 진영 최고 추천 댓글 상단 고정 (기획 항목, 구현 여부 미확인)
- 투표 전 댓글 작성 시도 시 처리 방식
- 페이지 로드 성능 (LCP, INP) — scroll-snap 전환 후 재측정 필요

---

## 10. 반영 결과 (2026-08-13)

### §6 캐시 — 이상 없음

문서가 우려한 상태가 아니었다. 실측:

```
Cache-Control: public, max-age=0, must-revalidate
X-Vercel-Cache: STALE          ← ISR이 백그라운드 재생성 중이라는 정상 신호
Age: 97
3회 연속 요청 → 모두 <title>오또케?! — 이거 나만 그래?</title>
OG 해시 ccae02562581e528       ← 문서가 본 bde1481818ad1f29 와 다름
```

`STALE`은 stale-while-revalidate 동작이고 반환된 내용은 최신이다.
`revalidate = 60`이라 캐시 항목이 60초를 넘기면 STALE로 내주면서 뒤에서 다시 만든다.
**구글봇이 보는 것도 최신이다.** 문서는 배포 전파 전에 작성된 것으로 보인다.

### §1 "미반영" 3개 — 실측 결과 모두 반영돼 있었다

sticky 헤더 · 카드 통계 줄(참여자·댓글 수) · 카드 내부 여백 축소. 재작업하지 않았다.
§5-4(`/q`·`/best` index)와 §9(투표 전 결과 노출·반대편 1위·투표 전 댓글 차단)도 이미 확인된 항목이다.

### §2 scroll-snap — 🔴 문서 지시에서 한 곳 벗어났다

**`mandatory` 대신 `proximity`를 썼다.**

§2-3은 `scroll-snap-type: y mandatory`를, §2-5는 *"카드 최소 높이는 100dvh,
내용이 더 길면 자연스럽게 늘어나도록"* 을 요구한다. **이 둘은 함께 성립하지 않는다.**
mandatory는 스크롤이 멈추는 순간 강제로 스냅 지점으로 끌어당기므로, 카드가 화면보다 길면
**중간 내용이 정지 상태로 도달 불가**가 된다. 긴 질문 + 결과 막대 + 통계가 겹치면 실제로 일어난다.
proximity는 스냅 감각을 유지하면서 긴 카드를 갇히게 하지 않는다.

그 외 §2는 지시대로:

- **스크롤을 가로채지 않는다**(§2-2). `overflow:hidden` + 버튼 전용 이동은 쓰지 않았다
- **문서(html)를 스크롤 컨테이너로** 썼다 — 별도 래퍼를 만들면 sticky 헤더와 푸터가
  컨테이너 밖으로 밀려 레이아웃을 다시 짜야 한다. 스냅 지점은 홈 피드만 내보내므로
  다른 페이지는 영향받지 않고, 클래스를 토글할 필요도 없다
- `100dvh` + `100vh` 폴백(§2-4) · `scroll-snap-stop: always`(§2-3)
- 슬라이드 높이를 `calc(100dvh - 7rem)`으로 두어 **다음 카드 상단이 걸친다**(§2-6)
- `scroll-margin-top`으로 sticky 헤더 아래에 정렬
- `prefers-reduced-motion`에서 스냅과 smooth를 모두 해제
- **키보드 핸들러는 붙이지 않았다.** ↑↓·PageUp/Down·Space는 네이티브가 이미 처리하고,
  핸들러를 붙이면 브라우저 기본 동작과 싸운다. 스냅이 걸려 있으니 결과도 동일하다
- 데스크톱 우측 위/아래 버튼은 `pointer: fine` + 900px 이상에서만 노출(§2-3, 보조 수단)
- **§2-8 적용 범위 준수** — 홈만 스냅. 상세는 슬라이드 0개로 확인

### §2-7 진행 인디케이터

`제 1호 · 전체 103건`. sticky 헤더 바로 아래에 붙는다.
화면 중앙을 지나는 슬라이드 하나만 현재로 잡는다(`rootMargin: -50% 0 -50%`) —
두 카드가 동시에 걸려 숫자가 흔들리는 것을 막는다.

### §3 댓글 — 이미 분리돼 있었고, 유도 문구만 추가

피드 카드에는 원래 댓글 본문이 없고 상세에만 있었다. 문서가 요구한 구조가 이미 맞다.
추가한 것은 **0개일 때 "왜 그런지 남기기"**로 작성을 유도하는 것.
클릭하면 상세의 댓글 섹션(`#왜-그런지`)으로 앵커 이동한다.

### §4 광고 — 방식 B 채택

**방식 A(광고 전용 슬라이드)는 쓰지 않았다.** 한 화면이 전면 광고면 이탈률이 크게 오른다.
방식 B — 같은 슬라이드 안, 카드 아래에 배너. 4장마다 하나.
질문 카드와 같은 종이 카드로 감싸고 "광고" 라벨을 붙인다.
환경변수가 없으면 아무것도 그리지 않는다(승인 전 빈 상자 방지).

### 아직 안 한 것

- **모바일 실기기에서 스냅 감각 확인** — 이게 제일 중요하다.
  `100dvh`·`scroll-snap-stop`은 실기기 스와이프에서만 판단할 수 있다
- LCP·INP 재측정(스냅 전환 후)
- 접근성 감사(axe)
- **실제 표 채우기** — 여전히 질문당 0~1표. 가짜 표는 넣지 않았다

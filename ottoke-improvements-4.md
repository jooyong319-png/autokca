# 오또케 — 개선 작업 문서 (4차)

> `autokca.com` / 작성 시점: 2026-08-13
> 3차 문서(`ottoke-improvements-3.md`)의 후속
> **주제: scroll-snap 전환은 완료. 카드 스케일이 따라오지 않아 발생한 문제 보정**

---

## 0. 현황 진단

### 반영된 것

- ✅ **scroll-snap 피드 전환** — 한 화면 = 한 카드 구조로 동작
- ✅ **진행 인디케이터** — "제 1 호 · 전체 103건"
- ✅ **상하 이동 버튼** — 우측 하단 ↑ / ↓
- ✅ **댓글 진입점** — "왜 그런지 남기기" 링크 추가
- ✅ 참여자 수, 표본 임계치 카피, 도장 플레이스홀더 유지

### 문제

**카드 크기가 이전(무한 스크롤) 기준 그대로입니다.**

- 카드 높이 약 400px / 뷰포트 약 900px → **위아래로 각각 약 250px씩 빈 공간**
- 이전에는 카드가 연속 배치되어 여백이 분산되었으나,
  한 화면에 한 장만 남기면서 **여백이 한곳에 집중**됨
- 결과적으로 전환 전보다 더 허전해 보임

> 한 화면 = 한 카드 구조는 **카드가 화면을 지배할 때만** 성립합니다.
> 현재는 넓은 화면 중앙에 작은 카드 한 장이 놓인 상태입니다.

---

## 1. 3차 문서 지침 중 취소할 항목 (중요)

### ❌ 취소: "카드 내부 여백 20~30% 축소" (2차 6-3, 3차 5-3)

이 지침은 **무한 스크롤 구조**를 전제로 작성되었습니다.
당시에는 카드가 연달아 나열되어 내부 여백이 넓으면 늘어져 보였습니다.

**scroll-snap 구조에서는 정반대입니다.** 카드가 화면을 채워야 하므로
내부 여백은 **축소가 아니라 확대**해야 합니다.

→ 해당 지침을 폐기하고, 아래 2장 기준으로 재작업하세요.

---

## 2. 카드 스케일 보정 (P0 · 핵심)

### 2-1. 카드 크기

```css
.card {
  width: min(860px, 92vw);
  min-height: min(78dvh, 760px);
  display: flex;
  flex-direction: column;
  justify-content: space-between;  /* 내부 요소를 상하로 분산 */
  padding: clamp(32px, 4vh, 56px) clamp(28px, 3.5vw, 48px);
}
```

- 폭: 약 680px → **860px**
- 높이: 내용 높이 → **뷰포트의 약 78%**
- `justify-content: space-between` 으로 내부 요소가 카드 전체에 퍼지게 함
  (단순히 높이만 키우면 상단에 몰리고 아래가 빈 채로 남음)

### 2-2. 질문 텍스트 확대

한 화면에 질문이 하나뿐이므로 **제목이 화면의 주인공**이어야 합니다.

```css
.question {
  font-size: clamp(28px, 4.5vw, 52px);
  line-height: 1.3;
  word-break: keep-all;
}
```

### 2-3. 선택지 행 · 도장 확대

- 선택지 행 높이: 약 64px → **90~100px**
- 선택지 텍스트: 18~20px → **22~26px**
- 도장 원 지름: 현재 약 40px → **56~64px**
  (현재 도장이 작아 컨셉의 핵심 요소인데도 존재감이 약함)
- 도장 칸 폭도 함께 확대

### 2-4. 하단 요소

- 참여자 수 / "왜 그런지 남기기" / 결과 문구 / 버튼을 카드 하단에 고정
- 버튼 크기도 소폭 확대 (터치 영역 48px 이상 확보)

---

## 3. 상하 여백 처리 (P1)

카드를 키워도 위아래에 약간의 여백은 남습니다. 이 공간을 비워 두지 말고
서류 컨셉으로 활용합니다.

### 3-1. 상단 — 진행 표시를 문서 머리글로

현재 "제 1 호 · 전체 103건"이 **좌측 상단에 단독으로 떠 있어** 붕 뜬 인상입니다.
헤더가 없어 소속이 불분명합니다.

**수정**

- 로고와 함께 **sticky 헤더로 통합** (3차 5-1 항목, 여전히 미반영)
- 또는 카드 상단 **중앙**에 배치하여 공문서 머리글처럼 처리
- 헤더 높이는 44~52px 이내

### 3-2. 하단 — 다음 카드를 걸치게

현재 카드 아래가 완전한 배경색이라 **"여기서 끝인가?"** 라는 인상을 줍니다.

**수정**

- 다음 카드 상단이 **5~8% 걸쳐 보이도록** 배치
- 그 위에 "↓ 다음 안건" 안내 텍스트 (옅은 색, 작게)
- 첫 진입 시에만 짧은 스크롤 힌트 애니메이션을 주는 것도 검토

> 이것이 이번 사이클에서 두 번째로 중요한 항목입니다.
> 다음이 있다는 신호가 없으면 첫 카드에서 이탈합니다.

---

## 4. 이동 버튼 개선 (P2)

현재 우측 하단 ↑ / ↓ 버튼이 작고 배경과 대비가 낮아 눈에 띄지 않습니다.

- 크기 확대 (최소 48×48px)
- **컨셉에 맞는 스타일** — 도장 테두리, 괘선, 종이 질감 등
- 대비 확보 (현재 어두운 배경에 어두운 버튼)
- 데스크톱에서만 노출, 모바일에서는 숨김 (스와이프로 충분)
- 키보드 ↑/↓/PageUp/PageDown/Space 지원 여부 확인

---

## 5. 우선순위

| 순위 | 항목 | 위치 | 비고 |
|---|---|---|---|
| 1 | 카드 크기 확대 (폭 860 / 높이 78dvh) | 2-1 | 체감 변화 가장 큼 |
| 2 | 질문 텍스트 · 선택지 · 도장 확대 | 2-2, 2-3 | 1번과 한 세트 |
| 3 | 다음 카드 5~8% 걸치게 | 3-2 | 이탈 방지 |
| 4 | sticky 헤더 + 진행 표시 통합 | 3-1 | 3차부터 미반영 |
| 5 | 이동 버튼 스타일·크기 | 4 | |

1~2번만 처리해도 인상이 크게 달라집니다.

---

## 6. 검증 방법

작업 후 아래를 확인하세요.

- [ ] 데스크톱(1900×900)에서 카드가 화면 세로의 **75~80%** 를 차지하는가
- [ ] 모바일(390×844)에서 카드가 잘리지 않고 한 화면에 들어오는가 (`100dvh` 적용 확인)
- [ ] 질문이 긴 카드에서도 레이아웃이 깨지지 않는가 (카드 내부 `overflow-y: auto`)
- [ ] 카드 하단에 다음 카드가 보이는가
- [ ] 스크롤·스와이프·키보드·버튼 네 가지 이동 수단이 모두 동작하는가

---

## 7. 계속 미반영 / 미확인 항목

3차 문서에서 넘어온 항목입니다.

- ❌ **sticky 헤더** (3차 5-1) — 4차 3-1로 통합
- ❌ **`/q`, `/best` index 허용** (3차 5-4)
- ⚠️ **CDN 캐시 확인** (3차 6장) — 미확인 상태
  ```bash
  curl -sI https://autokca.com/ | grep -i -E 'cache-control|age|x-vercel-cache'
  curl -s https://autokca.com/ | grep -o '<title>[^<]*</title>'
  ```
  반환된 title이 질문 제목이면 크롤러가 이전 HTML을 보고 있는 것
- ⚠️ **투표 전 결과 노출 여부** — 투표 전 퍼센트/막대가 보이면 안 됨
- ⚠️ 반대 진영 최고 추천 댓글 상단 고정 (기획 항목, 구현 여부 미확인)
- ⏸ 인피드 광고 (3차 4장) — 애드센스 승인 후 착수

---

## 8. 유지할 것 (훼손 금지)

- scroll-snap 구조 자체 — 방향은 정확함. 크기만 보정하면 됨
- 투표용지 / 공문서 컨셉 — 크림색 종이 카드, 테두리 선택지 표, 빨간 도장
- 도장 플레이스홀더(점선 원)
- "제001-8255호 안건 / 별건" 문서번호 체계
- 사극 말투 선택지
- 표본 임계치 로직 (20표 미만 퍼센트 숨김 / 50표 미만 판정 카피 숨김)
- 참여자 수 상시 노출
- "왜 그런지 남기기" 댓글 진입점
- 배경 격자 텍스처
- 하단 면책 문구 — "여론조사가 아닙니다"

---

# 반영 결과 (2026-08-13)

## 반영 완료

| 항목 | 무엇을 했나 |
|---|---|
| §1 여백 축소 **취소** | `.ballot` 패딩 `1.2rem/1.35rem` → `clamp(1.5rem,3.4vh,3.2rem) clamp(1.35rem,3.2vw,2.9rem)`. 2·3차의 축소 지침 주석을 폐기 근거와 함께 교체 |
| §2-1 카드 크기 | 홈만 `.stage` 894px → **카드 실폭 ≈ 859px**. 높이는 아래 "다르게 한 판단 ①" |
| §2-1 내부 분산 | `.question`·`.stats`에 `margin-top: auto` → 머리글 / 질문·기표칸 / 집계·버튼 3구역 |
| §2-2 질문 확대 | `clamp(1.55rem, 6.4vw, 2.1rem)` → `clamp(1.7rem, 4.4vw, 3rem)` (상한 34px → **48px**) |
| §2-3 선택지·도장 | 선택지 `1.02rem` → `clamp(1.2rem,3.6vw,1.55rem)` · 행 높이 62px → `clamp(74px,10.5vh,98px)` · 도장 44px → `clamp(48px,6.2vw,60px)` · 도장칸 폭 92px → `clamp(84px,24%,130px)` |
| §2-4 하단 요소 | `.stats`부터 카드 하단 고정. 버튼 `min-height: 48px` + 폰트 0.82→0.9rem |
| §3-1 진행 표시 | **sticky 헤더 안으로 포털 이동.** 로고 반대쪽 끝에 앉는다 |
| §3-2 다음 카드 걸침 | 슬라이드 `100dvh - 7rem` → `- 8.5rem` → **걸침 7.5%** (기존 4.8%) |
| §3-2 스크롤 힌트 | "↓ 다음 안건" — **첫 슬라이드에만.** 103장 전부면 소음이다. reduced-motion에서 애니메이션 해제 |
| §4 이동 버튼 | 2.2rem(35px) → **48×48px**. 기표칸과 같은 조판(2px 잉크 외곽선 + 1px 내부 괘선 한 덩어리), hover에 인주색 반전 |

### §2-3의 부수 수정 — 도장 크기를 토큰으로 묶었다

`.slot`(점선 자리)과 `.seal`(찍힌 도장)이 각각 `44px`로 **따로** 박혀 있었다.
한쪽만 키우면 찍히는 순간 크기가 튄다. `--stamp` 하나를 공유하게 바꿨다.

### §4 대비 문제의 실제 원인

문서는 "어두운 배경에 어두운 버튼"이라고 적었는데, 원인은 반대였다.
버튼 배경 `--paper`(#fcfbf8)와 방 배경 `--room`(#e9e5dc)이 **라이트 모드에서 둘 다 밝은 크림색**이라
1px `--paper-edge` 테두리로는 분리가 안 됐다. 다크에서는 밝은 종이가 어두운 방에 얹혀 잘 보였다 —
그래서 놓치기 쉬웠다. `--ink`는 두 테마 모두 어두우므로(종이는 항상 밝다) 2px 잉크 외곽선으로 양쪽에서 해결된다.

## 문서와 다르게 한 판단 3건

### ① 카드 높이를 `min(78dvh, 760px)`로 고정하지 않고 **슬라이드의 잔여 높이**로 줬다

`.slide > section:first-child { flex: 1 1 auto; }`

카드에 높이를 직접 박으면 **4장마다 붙는 광고 슬라이드**에서 카드+광고가 화면을 넘어가
다음 카드가 안 걸친다(§3-2가 깨진다). flex로 "남은 만큼"만 주면 광고가 있든 없든 총 높이가 유지된다.

### ② `overflow-y: auto`(§6 검증항목)를 넣지 않았다

카드 안에 또 스크롤 영역을 만들면, `mandatory`를 버리고 `proximity`를 쓴 이유
(**긴 카드의 중간 내용이 정지 상태로 도달 불가**)를 카드 안에서 그대로 재현한다.
도장 애니메이션이 `scale(2.3)`에서 시작하므로 잘리는 문제도 생긴다.
→ 내용이 길면 **슬라이드가 늘어나게** 둔다. 긴 질문 최장 35자는 860px 카드에서 3줄이라 넘치지 않는다.

### ③ 카드를 화면 세로의 **85%**로 했다 (§6은 75~80%)

§6과 §3-2가 sticky 헤더를 감안하면 **동시에 성립하지 않는다.** 1900×900 기준:

```
헤더 51px(5.7%) + 카드 + 카드간격 17.6px(2%) + 걸침 = 900
```

- 카드를 78%(702px)로 맞추면 → 걸침이 **13%** (§3-2의 5~8% 초과)
- 걸침을 7.5%로 맞추면 → 카드는 **85%**

걸침을 택했다. 문서가 "§3-2는 이번 사이클 두 번째로 중요하다"고 못 박았고,
75~80%는 "카드가 화면을 지배한다"의 대리 지표인데 85%면 그 목적은 더 잘 달성된다.
→ 결과: 카드 84.9% · 걸침 7.5%. 모바일 390×844도 83.9% · 7.9%로 같이 맞는다.

아주 높은 화면(1440px+)에 상한을 두지 않았다. 상한을 두면 카드를 다시 슬라이드 중앙에
띄워야 해서 이번에 고친 문제가 되살아나고, `max-height`는 긴 질문에서 잘라낼 수도 없다(②).

## §7 항목 실측 — 5건 중 4건이 이미 되어 있었다

> ⚠️ **네 사이클 연속으로 "미반영" 목록이 사실과 달랐다.** 이번에도 측정이 먼저였다.

| 문서 표기 | 실측 |
|---|---|
| ❌ sticky 헤더 (3차 5-1) | **이미 반영돼 있었다.** `.masthead-bar { position: sticky; top: 0 }`. 이번엔 진행 표시까지 그 안으로 넣어 §3-1이 완결됐다 |
| ❌ `/q`, `/best` index 허용 | **이미 허용 상태였다.** `noindex`를 내는 곳은 `/me`(의도적)와 thin 질문 페이지뿐이다 |
| ⚠️ CDN 캐시 확인 | **정상.** `X-Vercel-Cache: PRERENDER` · `Age: 0` · title이 `오또케?! — 이거 나만 그래?`(브랜드 제목). 크롤러가 최신 HTML을 본다 |
| ⚠️ 투표 전 결과 노출 | **노출 없음.** 서버 HTML에 막대 클래스 0회, "응답자" 0회 |
| ⚠️ 반대 진영 1위 상단 고정 | **구현돼 있었다.** `Comments.tsx`에서 섹션 머리글 직후, 작성 폼·양쪽 칼럼보다 **위**에 렌더된다 |
| ⏸ 인피드 광고 | 애드센스 승인 후 — 그대로 보류 |

## §8 유지 항목 — 훼손 없음

scroll-snap 구조 · 투표용지 컨셉 · 점선 도장 자리 · 문서번호 체계 · 사극 말투 ·
표본 임계치(20/50) · 참여자 수 상시 노출 · "왜 그런지 남기기" · 배경 격자 · 면책 문구
— 전부 그대로다. **가짜 표를 넣지 않았고 DB를 건드리지 않았다.**

## 검증

- `npx tsc --noEmit` 통과
- `npm run build` 통과 (125개 페이지 · 질문 103개 프리렌더)
- 빌드 CSS에 `:has(.snap-feed)` · `894px` · `--stamp` · `.slide>section:first-child` 전부 반영 확인
- 홈 렌더: `snap-feed` 1 · `feed-progress-slot` 1 · 힌트 1(첫 장만) · 막대 0 · "응답자" 0
- 상세 렌더: `snap-feed` 0 · 슬라이드 0 → 720px 유지, 카드가 늘어나지 않음(댓글이 접히면 안 된다)

### 남은 것 — 실기기 확인 (§6)

치수는 계산으로 맞췄지만 **눈으로 본 것은 아니다.** 3000포트에서 확인이 필요하다:

- [ ] 데스크톱에서 카드가 화면을 지배하는가 (계산상 85%)
- [ ] 모바일에서 카드가 잘리지 않는가 — 특히 선택지 11자("다수결로 정해야 한다")가 2줄로 접히는 모양
- [ ] 다음 카드가 걸쳐 보이는가 (계산상 7.5%)
- [ ] 도장 60px이 과하지 않은가
- [ ] 헤더 안 진행 표시가 로고와 한 줄로 읽히는가

---

## 후속 수정 (2026-08-13) — 헤더가 카드 머리를 덮던 문제

사용자 보고: *"헤더 스티키 넣은건 알겠는데, 가려지는데? 아마 z index 문제같음"*

**z-index 문제가 아니었다.** 헤더 `z-index: 10`이 최상위이고 그게 정상이다.
실제 원인은 **여유가 0.41px밖에 없었던 것**이다.

| | px |
|---|---|
| 워드마크 라인박스 (1.32rem × line-height 1.6) | 33.79 |
| `.masthead-bar` 패딩 (0.55 + 0.45rem) | 16.00 |
| 스크롤 시 붙는 `border-bottom` | 1.00 |
| **헤더 실측** | **50.79** |
| `scroll-margin-top: 3.2rem` (하드코딩) | 51.20 |
| **여유** | **0.41** |

Windows 디스플레이 배율 125·150%에서 이 0.41px가 반올림되며 헤더가 카드 머리를 덮었다.

**카드를 키운 것이 원인을 노출시켰다.** 이전에는 카드가 슬라이드 중앙에 떠서 스냅 경계보다
195px 아래에 있었으므로 오프셋이 몇 px 틀려도 드러나지 않았다.

### 고친 방법 — 두 값을 한 토큰으로 묶었다

```css
:root {
  --header-h: 3.2rem;
  --header-gap: 0.7rem;
  --header-clear: calc(var(--header-h) + var(--header-gap));
}
.masthead-bar { min-height: var(--header-h); }   /* 헤더가 이보다 커질 수 없다 */
.slide        { scroll-margin-top: var(--header-clear); }
```

→ 여유 **0.41px → 11.2px**. 헤더 높이를 `min-height`로 고정했으므로 앞으로 헤더에 무엇을
넣어도 스냅 오프셋을 넘어서지 못한다.

### 같은 원인으로 앵커 이동도 가려져 있었다 (보고되지 않은 버그)

`#왜-그런지`(댓글 섹션 제목)와 `#<슬러그>`(상세의 "다음 질문 →")에는
`scroll-margin`이 **아예 없었다.** 앵커로 점프하면 대상이 y=0에 붙어 제목이 헤더에 가렸다.

```css
:target { scroll-margin-top: var(--header-clear); }
```

한 줄로 전역 처리했다.

### 세로 배분 갱신

| | px | % |
|---|---|---|
| sticky 헤더 | 51.2 | 5.7 |
| 헤더 여유 (신규) | 11.2 | 1.2 |
| **카드** | **764** | **84.9** |
| 카드 간격 | 17.6 | 2.0 |
| **다음 카드 걸침** | **56** | **6.2** |

걸침이 7.5% → 6.2%로 줄었지만 §3-2의 5~8% 구간 안이다.

검증: `.next` 삭제 후 재빌드 통과 · 빌드 CSS에서 `--header-h`가 한 번만 정의되고
`min-height`·`scroll-margin-top`(2곳)·`:target`이 모두 `var()`로 참조 · 하드코딩 잔존 없음.

---

## 후속 수정 2 (2026-08-13) — 카드 간격과 도장 위치

사용자가 화면에서 두 가지를 잡아냈다. 둘 다 버그였다.

### ① 카드 사이 간격이 0이었다 (그리고 계산도 틀렸다)

*"맨위 꺼랑 그 밑꺼랑 공간이 있거든? 근데 그 밑꺼랑 그 밑밑꺼는 공간이 없어"*

🔴 위 반영 결과의 세로 배분 표에 **"카드 간격 17.6px(`.stage` gap 1.1rem)"이라고 적었는데
그런 간격은 존재하지 않았다.** 슬라이드는 `.stage`의 직계 자식이 아니라
Feed 래퍼(`div.snap-feed`) 안에 있고, 그 래퍼는 flex가 아니라 gap이 적용되지 않는다.
**카드끼리 맞붙어 있었다.**

그런데 첫 카드 아래에만 공간이 있었던 건 **"↓ 다음 안건" 힌트가 슬라이드 흐름 안에 있어서**
첫 카드만 그만큼 짧아졌기 때문이다. 그래서 1↔2에는 공간이 있고 2↔3에는 없었다.

**고친 방법**

- `.slide { position: relative; margin-bottom: 1.6rem }` — 간격을 슬라이드가 스스로 갖는다.
  `margin`은 스냅 정렬에 영향을 주지 않는다(그건 `scroll-margin`이다)
- `.cue`를 `position: absolute; bottom: -1.45rem`로 그 간격 안에 앉혔다 →
  모든 카드 높이가 같아진다
- 슬라이드 높이 `100dvh - 8.5rem` → `- 9.5rem`으로 걸침 재조정

| 화면 | 카드 | 걸침 |
|---|---|---|
| 1900×900 | 748px (83.1%) | 64px (7.1%) |
| 390×844 | 692px (82.0%) | 64px (7.6%) |
| 1080 세로 | 928px (85.9%) | 64px (5.9%) |

걸침이 64px로 일정하고 세 해상도 모두 §3-2의 5~8% 구간 안이다.

### ② 도장이 찍혔을 때와 안 찍혔을 때 위치가 달랐다

*"이거 '오' 도장 위치 찍혔을떄랑 안찎혓을떄 위치 다른거 의도한거야?"*

의도가 아니었다. `.slot`(점선 자리)과 `.seal`(도장)은 **둘 다 항상 DOM에 있고**,
안 찍힌 상태의 `.seal`은 `opacity: 0`일 뿐 **레이아웃 자리는 그대로 차지한다.**
flex row라서 도장 두 개가 나란히 놓였다:

| | 결과 |
|---|---|
| 데스크톱 (`.box` 130px · 도장 60px) | 두 개 120px → 점선 원이 정중앙에서 **30px 왼쪽** |
| 모바일 (`.box` 84px · 도장 48px) | 두 개 96px > 84px → 각각 **42px로 찌그러져 42×48 타원** |
| 찍은 순간 | `.slot`이 `display: none` → 도장만 정중앙 → **위치가 튄다** |

도장을 44px → 60px로 키우면서 어긋남이 22px → 30px로 커져 눈에 띈 것이고,
**원인 자체는 4차 작업 전부터 있었다.**

**고친 방법** — 같은 grid 칸에 겹친다:

```css
.box { display: grid; place-items: center; }
.box > * { grid-area: 1 / 1; }
```

무엇이 보이든 항상 같은 자리다. 모바일 찌그러짐도 함께 해결된다(grid는 flex처럼
아이템을 줄이지 않는다).

> 교훈: **`opacity: 0`은 자리를 비우지 않는다.**

검증: `.next` 삭제 후 재빌드 통과 · 컴파일된 CSS에서 `display:grid` ·
`grid-area:1/1` · `.slot`과 `.seal`이 모두 `var(--stamp)` 확인.


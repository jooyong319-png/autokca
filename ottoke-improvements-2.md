# 오또케 — 개선 작업 문서 (2차)

> `autokca.com` / 검증 시점: 2026-08-13
> 검증 범위: 홈(모바일·데스크톱 스크린샷), 질문 상세(`/q/축의금-5만원`) 서버 HTML
> 1차 문서(`ottoke-improvements.md`)의 후속. 1차에서 처리된 항목은 8장에 정리

---

## 0. 요약

1차 개선 사항은 **상세 페이지에 대부분 정확히 반영**되었습니다.
이번 문서는 두 갈래입니다.

- **A. 미반영 / 누락 항목** (1~2장) — 반드시 처리
- **B. 데스크톱 밀도 개선** (3~6장) — "화면이 허전하다"는 문제의 원인과 대응

모바일은 현재 상태로 문제 없습니다. 줄바꿈, 임계치 카피 모두 정상 동작 확인.

---

## 1. 홈 페이지 미반영 (P0)

### 1-1. 홈이 stale 상태 — 데이터가 갱신되지 않음

**증거**: 동일 질문(`축의금-5만원`)이 경로에 따라 다른 값을 출력합니다.

| 위치 | 출력 | "도장을 찍어 주세요" |
|---|---|---|
| 홈 (`/`) | "아직 아무도 안 눌렀습니다" (0표) | 없음 |
| 상세 (`/q/축의금-5만원`) | "개표 중입니다. 아직 1표라…" | 있음 |

홈 HTML이 이전 검증 시점과 완전히 동일하며, OG 이미지 해시(`bde1481818ad1f29`)까지 같습니다.
홈이 빌드 시점에 정적 생성된 뒤 재검증되지 않고 있습니다.

**수정**

```ts
// app/page.tsx
export const revalidate = 60;
// 또는 투표 수를 실시간 반영해야 하면
// export const dynamic = 'force-dynamic';
```

투표 수처럼 계속 변하는 데이터가 있는 페이지는 ISR 주기를 짧게 잡거나 동적 렌더링으로 전환합니다.
피드 카드가 클라이언트에서 갱신되는 구조라면, **최소한 서버 렌더링된 초기 HTML의 수치가
실제와 어긋나지 않도록** 해야 합니다 (크롤러가 보는 값이기도 함).

### 1-2. 홈 메타 태그 분리 (1차에서 누락)

현재 홈 메타:

```
title: 친구 결혼식 축의금 5만원이면 적나요?
og:title: 친구 결혼식 축의금 5만원이면 적나요?
og:image: /opengraph-image?bde1481818ad1f29
canonical: https://autokca.com
```

홈이 첫 번째 질문의 제목을 그대로 사용하고 있습니다.

**이 문제가 현재 더 중요해진 이유**: 모든 질문 페이지에 `noindex`가 적용되어 있어
**색인 가능한 페이지가 사실상 홈 하나뿐**입니다. 그 유일한 페이지의 제목이 질문 제목입니다.

**수정**

```
title: 오또케?! — 이거 나만 그래?
description: (사이트 소개 문구)
og:title: 오또케?! — 이거 나만 그래?
og:image: 사이트 대표 이미지 (질문별 OG는 /q/slug가 담당)
```

---

## 2. 색인 정책 조정 (P1)

### 2-1. 현재 상태 확인

- 모든 질문 페이지: `noindex, follow` → 의도대로 동작 중
- 결과적으로 **현재 검색 유입 가능 경로가 없음**
- 초기 트래픽은 100% 커뮤니티 시딩에 의존

이는 설계대로이며 문제가 아닙니다. 투표와 댓글이 쌓여 페이지가 두터워진 뒤 색인되는 것이 정석입니다.
다만 "론칭했는데 검색에 안 나온다"는 것이 정상 상태임을 인지해야 합니다.

### 2-2. 목록 페이지는 index로 열 것

- `/q` (질문 전체), `/best` (집계) → **`index` 허용**
- 개별 질문이 얇아도 목록 페이지는 텍스트량이 충분함
- 목록이 색인되면 `follow`를 통해 크롤러가 개별 질문까지 도달
- `sitemap.xml`에는 목록 페이지 + 임계치 통과 질문만 포함

---

## 3. 광고 전략 수정 (P1) — 기존 방향 정정

### 이전 판단의 오류

1차 기획에서 "세션당 PV가 매출을 결정한다"고 설정했으나,
**현재 구조는 무한 스크롤이므로 아무리 많이 스크롤해도 1 PV입니다.**
상세 페이지 하단에도 피드가 붙어 있어 페이지 이동이 거의 발생하지 않습니다.

### 수정된 방향 — PV 최적화가 아니라 노출 최적화

애드센스는 **광고 노출(impression) 기준**이므로 무한 스크롤이 오히려 유리합니다.

**인피드 광고 설계**

- 질문 카드 **3~4개마다 광고 슬롯 1개** 삽입
- 무한 스크롤로 새 카드를 로드할 때 광고 슬롯도 함께 주입
- 광고 카드도 **질문 카드와 동일한 종이 카드 스타일**로 감싸서 리듬을 유지
- 사이드바 고정 광고는 넣지 않음 (스크롤 리듬 훼손 + 데스크톱 전용이라 효율 낮음)

**주의**: 화면 대비 광고 비율 정책이 있으므로 카드 3~4개당 1개가 안전선입니다.
과밀 배치 시 애드센스 정책 위반 위험이 있습니다.

---

## 4. 데스크톱 밀도 — 원인 1: 빈 도장 칸 (P1)

### 현상

투표 전 상태의 선택지 표에서 도장 칸이 완전히 비어 있습니다.

```
┌──────────────────────┬────────┐
│ 바꾼다               │        │  ← 빈 칸
├──────────────────────┼────────┤
│ 안 바꾼다            │        │  ← 빈 칸
└──────────────────────┴────────┘
```

- 표가 잘못 렌더링된 것처럼 보임
- 첫 방문자는 투표 전 상태만 보므로 이것이 첫인상이 됨
- 허전함의 가장 큰 원인

### 수정

빈 칸에 **도장 자리 플레이스홀더**를 배치합니다.

- 회색 **점선 원** (`border: 1.5px dashed`, 옅은 회색)
- 원 안에 "오" 글자를 매우 낮은 투명도(0.12~0.18)로
- hover / tap 시 투명도를 0.3~0.4로 올려 프리뷰
- 클릭 시 실제 도장(빨강)으로 전환

**효과**: 허전함 해소 + 클릭 가능성 전달이 동시에 해결됩니다.
현재 텍스트로 안내하는 "도장을 찍어 주세요"를 시각 요소가 대체하므로,
해당 문구는 더 작게 하거나 제거해도 됩니다.

---

## 5. 데스크톱 밀도 — 원인 2: 카드 정보량 부족 (P1)

현재 카드 구성 요소: 문서번호 / 구분(안건·별건) / 질문 / 선택지 / 결과 문구 / 버튼 2개.
여백이 넓은 것이 아니라 **표시할 정보 자체가 부족**한 상태입니다.

### 추가할 요소

| 요소 | 표기 예 | 우선순위 | 비고 |
|---|---|---|---|
| **댓글 수** | "왜 그런지 12" | **높음** | 상세 페이지 유입 동기. 현재 없음 |
| **참여자 수** | "37명 참여" | **높음** | 임계치 미만이어도 노출 가능 (퍼센트만 감추면 됨) |
| 태그 | "직장 / 결혼 / 위생" | 중간 | 문서번호 옆에 작게 |
| 경과 시간 | "3일째 개표 중" | 낮음 | 공문서 톤과 어울림 |

**댓글 수는 반드시 추가하세요.** 현재 카드에서 상세 페이지로 넘어갈 동기가 약한 주된 이유입니다.
댓글은 체류 시간이자 SEO 본문이므로, 상세 유입이 곧 사이트 성장입니다.

**참여자 수 관련 규칙 정리** (임계치 로직 보완)

- 참여자 수(N명): **항상 노출**
- 퍼센트 / 막대그래프: 20표 이상
- 판정 카피("무난하네요" 등): 50표 이상

---

## 6. 데스크톱 밀도 — 원인 3: 레이아웃 (P2)

### 6-1. 카드 폭 확대

- 현재 약 590px / 뷰포트 1900px → 좌우 배경이 화면의 약 65%
- **720~780px로 확대** 권장
- 질문 제목이 한 줄에 들어가는 경우가 늘어 리듬도 개선됨

### 6-2. 배경 장식 (컨셉 강화)

단색 배경(`#131210` 계열)이라 여백이 더 비어 보입니다. 컨셉에 맞는 방향:

- 아주 옅은 **격자무늬 / 종이결 텍스처**
- 좌우에 **서류철 펀치홀**을 세로로 배열 (바인더에 끼운 문서 느낌)
- 세로 텍스트로 "오또케 투표소 · 제1호" 등을 낮은 투명도로 배치

**주의**: 인피드 광고(3장)가 들어가면 좌우 여백의 체감이 줄어듭니다.
배경 장식에 과도한 공수를 들이지 말고, 광고 배치 이후에 재평가하세요.

### 6-3. 카드 내부 여백 축소

선택지 표 → 결과 문구 → 버튼 사이 수직 간격이 넓어 카드가 늘어져 보입니다.
**20~30% 축소**하면 한 화면에 들어오는 카드 수가 늘고 밀도가 개선됩니다.

### 6-4. sticky 헤더

현재 스크롤 시 헤더가 사라져, 무한 스크롤로 내려가면 사이트 정체를 알 수 없게 됩니다.
로고만 포함한 얇은 sticky 헤더를 상단에 고정합니다. 브랜드 각인에도 유리합니다.

### 6-5. 2열 배치 — 현재는 보류

데스크톱에서 카드를 2열로 배치하면 밀도는 크게 개선되나,
무한 스크롤의 리듬이 깨지고 반응형 분기가 늘어납니다.
질문이 수백 개 쌓이고 "훑어보는" 사용 패턴이 생긴 뒤에 재검토합니다.

---

## 7. 작업 우선순위

| 순위 | 항목 | 위치 |
|---|---|---|
| 1 | 홈 `revalidate` / 동적 렌더링 설정 | 1-1 |
| 2 | 홈 메타 태그 분리 | 1-2 |
| 3 | 빈 도장 칸에 점선 플레이스홀더 | 4 |
| 4 | 카드에 댓글 수 · 참여자 수 추가 | 5 |
| 5 | 인피드 광고 슬롯 설계 | 3 |
| 6 | `/q`, `/best` index 허용 | 2-2 |
| 7 | 카드 폭 720~780px | 6-1 |
| 8 | 카드 내부 여백 축소 | 6-3 |
| 9 | sticky 헤더 | 6-4 |
| 10 | 배경 텍스처 · 펀치홀 장식 | 6-2 |

3번과 4번만 처리해도 데스크톱 체감이 크게 달라집니다.

---

## 8. 1차 문서에서 반영 확인된 항목 (재작업 불필요)

상세 페이지 기준으로 아래는 정상 반영되었습니다. 건드리지 마세요.

- ✅ `word-break: keep-all` — 모바일에서 "축의금 / 5만원이면" 정상 분리 확인
- ✅ 표본 임계치 카피 — "개표 중입니다. 아직 1표라 발표할 수 없습니다."
- ✅ "도장을 찍어 주세요" 안내
- ✅ 상세 페이지 canonical 분리
- ✅ `noindex, follow` 적용
- ✅ 상세 첫 카드에 "다음 질문 →", 이후 카드에 "이 질문만 보기 →" (구분 정확)
- ✅ 상세 페이지 하단 무한 스크롤 피드
- ✅ 질문별 전용 OG 이미지 경로 (`/q/slug/opengraph-image`)
- ✅ description 후킹 문구 — "당신은 어느 쪽인가요?"
- ✅ 댓글 진영 분리 표시 (적다 0 / 괜찮다 0)
- ✅ title 브랜드 접미 — "— 오또케?!"
- ✅ 태그라인 위치 이동 (로고 하단)

### 유지할 것 (개선 작업 중 훼손 금지)

- 투표용지 / 공문서 컨셉 — 크림색 종이 카드, 테두리 선택지 표, 빨간 도장
- "제001-8255호 안건 / 별건" 문서번호 체계
- 사극 말투 선택지 — "적다오 / 괜찮다오"
- 댓글 섹션 "왜 그런지" 및 진영 분리
- 댓글 안내 카피 — "링크와 연락처는 막습니다. 광고는 티가 납니다."
- 하단 면책 문구 — "여론조사가 아닙니다"

---

## 9. 미검증 — 추가 확인 필요

- **투표 전 결과 노출 여부**: 투표하기 전에 퍼센트/막대가 보이면 안 됨
  (참여 동기 상실 + 밴드왜건으로 데이터 왜곡)
- 반대 진영 최고 추천 댓글 상단 고정 (기획 항목, 구현 여부 미확인)
- 투표 전 댓글 작성 시도 시 처리 방식
- 페이지 로드 성능 (LCP, INP)

---

## 10. 운영 (개발 외)

- **실제 투표 채우기**: 현재 질문당 0~1표. 지인·단톡방을 통해 질문당 20~30표 확보.
  가짜 표는 절대 넣지 말 것 ("집계는 실제 투표 수" 명시했으므로 신뢰 훼손)
- **피드 정렬**: 초기에는 투표 수 많은 순. 표본이 쌓인 뒤 최신순/랜덤 전환
- **사이트 전체 누적 수치**: 헤더 또는 푸터에 "지금까지 N표" 표시

---

## 11. 반영 결과 (2026-08-13)

### 먼저 — 문서가 구 배포를 본 항목이 넷 있다

실측했더니 §7 우선순위의 **1·2·6번은 이미 반영돼 있었다.** 1차 개선을 배포한 직후
문서가 작성된 것으로 보인다. 재작업하지 않았다.

| 문서 항목 | 실측 결과 |
|---|---|
| §1-2 홈 메타 미반영 | ✅ 이미 `오또케?! — 이거 나만 그래?` · OG도 사이트 카드로 분리됨 |
| §1-1 홈 stale | ✅ 홈·상세 모두 "아직 1표". `revalidate = 60`이 이미 있었다 |
| §2-2 목록 index | ✅ `/q`·`/best` 모두 robots 태그 없음(= index) |
| §9 투표 전 결과 노출 | ✅ 홈·상세 모두 막대 0개 |
| §9 반대편 1위 댓글 | ✅ 구현됨. 댓글이 0개라 표시 조건 미충족일 뿐 |
| §9 투표 전 댓글 작성 | ✅ `403 "투표부터 하세요."` |

### 실제로 반영한 것

**§4 빈 도장 칸 — 허전함의 최대 원인**
점선 원 플레이스홀더를 놓았다. 안이 비어 "표가 깨진 것처럼" 보이던 문제.
hover·focus에서 인주색으로 진해지고, 찍히면 플레이스홀더가 사라지고 실선 도장이 남는다.
시각 요소가 말을 대신하므로 "도장을 찍어 주세요" 문구는 더 작고 조용하게 줄였다.

**§5 카드 정보량 — 참여자 수·댓글 수**
`N명 참여 · 왜 그런지 N`. 임계치 규칙을 문서대로 정리했다:

| 정보 | 노출 조건 |
|---|---|
| **참여자 수** | **항상** — 감출 것은 퍼센트지 참여 규모가 아니다 |
| 퍼센트·막대 | 20표 이상 **그리고** 투표한 뒤 |
| 판정 카피 | 50표 이상 |

댓글 수는 상세 페이지로 링크한다. 댓글이 체류시간이자 SEO 본문이라 상세 유입이 곧 성장이다.
홈·상세·피드 API 세 곳에 `readAllCommentCounts()`로 한 번에 실어 보낸다
(질문마다 부르면 100번 왕복한다).

**§6 레이아웃**
- 카드 폭 620 → **720px**
- 카드 내부 여백 약 25% 축소(`1.6/1.15rem` → `1.2/0.85rem`)
- **로고 줄만 sticky** — 태그라인까지 고정하면 화면을 잡아먹는다.
  스크롤이 시작되면 경계선이 드러난다(`animation-timeline: scroll()`, 미지원 브라우저는 선 없음)
- `--rule-strong` 토큰을 정식 추가했다. 점선 도장 자리가 폴백값을 쓰고 있어 다크 모드에서 어긋났다

**§3 인피드 광고 — 전략 정정을 코드에 기록**
문서의 지적이 맞다. 브리프는 "세션당 PV가 매출을 결정한다"고 했지만
**무한 스크롤이면 아무리 스크롤해도 1 PV**다. 목표는 PV가 아니라 노출이고,
애드센스는 노출 기준이라 무한 스크롤이 오히려 유리하다. 이 정정을 `AdSlot.tsx` 주석에 남겼다.

- 카드 **4개마다** 슬롯 1개. 그 이상은 화면 대비 광고 비율 정책에 걸린다
- 광고도 **질문 카드와 같은 종이 카드**로 감싼다. 리듬이 깨지면 스크롤이 멈춘다
- "광고" 라벨을 붙인다 — 콘텐츠로 오인되면 정책 위반이고 신뢰도 깎인다
- 🔴 **환경변수가 없으면 아무것도 그리지 않는다.** 승인 전에 자리만 잡으면
  빈 상자가 카드 사이에 끼어 광고보다 나쁜 상태가 된다.
  승인 후 `NEXT_PUBLIC_ADSENSE_CLIENT`·`NEXT_PUBLIC_ADSENSE_SLOT`을 넣으면 켜진다
- 사이드바 고정 광고는 넣지 않았다(문서 지시대로)

### 안 한 것

- **§6-2 배경 펀치홀·세로 텍스트** — 문서가 "광고 배치 이후 재평가"라고 했다.
  괘선 격자만 유지한다. 광고가 들어가면 좌우 여백 체감이 줄어든다
- **§6-5 2열 배치** — 문서대로 보류
- LCP·INP, 접근성 감사(axe)
- **§10 실제 표 채우기** — 운영 항목. 가짜 표는 넣지 않았다

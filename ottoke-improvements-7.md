# 오또케 — 개선 작업 문서 (7차)

> `autokca.com` / 작성 시점: 2026-08-13
> 6차 문서(`ottoke-improvements-6.md`)의 후속
> **주제: 투표 전후 흐름 설계 — 번복 · 댓글 유도 · 피드 정렬 · 헤더 내비게이션**

---

## 0. 이 문서의 범위

지금까지의 문서가 "화면이 어떻게 보이는가"였다면, 이 문서는 **"사용자가 어떻게 움직이는가"** 를 다룹니다.
네 항목 모두 투표 전후 동선에 관한 것이므로 한 묶음으로 작업하세요.

| 장 | 항목 |
|---|---|
| 2 | 투표 번복 허용 |
| 3 | 투표 직후 댓글 유도 |
| 4 | 피드 정렬 + `/me` 아카이브 확장 |
| 5 | 헤더 내비게이션 |

---

## 1. 전제 — 6차 임계치 조정이 먼저다

**이 문서의 3장(댓글 유도)은 6차 1장(임계치 하향)이 선행되어야 작동합니다.**

투표 직후 "개표 중입니다. 아직 4표라 발표할 수 없습니다"만 뜨는 상태에서
댓글 입력창을 띄우면, 사용자는 **무엇에 반응해서 글을 쓰라는 것인지 알 수 없습니다.**

결과 공개 → 반응 → 댓글 작성의 순서가 성립해야 하므로,
**결과 공개 임계치 5표 조정과 반드시 함께 배포하세요.**

---

## 2. 투표 번복 허용

### 2-1. 결론

**번복을 허용합니다.** 단 집계는 항상 1인 1표를 유지합니다.

### 2-2. 근거

1. **오터치 복구 불가** — scroll-snap 피드에서 스크롤 중 오터치가 발생함.
   되돌릴 수 없으면 첫 경험에서 이탈
2. **댓글까지 막힘** — 투표한 진영으로만 댓글을 쓸 수 있으므로,
   잘못 누르면 **반대 진영에 갇혀 발언 자체가 봉쇄**됨. 실수 하나가 참여를 완전히 차단
3. **데이터 품질 향상** — 번복을 막으면 "잘못 눌렀지만 어쩔 수 없는 표"가 섞임
4. **마음이 바뀌는 것이 이 사이트의 재미** — 댓글을 읽고 생각이 바뀌는 경험이
   핵심 가치인데 표를 못 바꾸면 모순

### 2-3. 집계 규칙

```
번복 시:
  이전 진영 카운트 −1
  새 진영 카운트 +1
  총 참여자 수 불변
```

- 이전 선택은 **httpOnly 쿠키에서 읽는다** (클라이언트 값 신뢰 금지 — 기존 원칙 유지)
- 누른 횟수만큼 표가 늘어나면 안 됨
- `lib/votes.ts` 에 차감 로직 추가 필요

### 2-4. 댓글이 있을 때는 번복 불가

댓글 작성 후 진영을 바꾸면 처리 방법이 마땅치 않습니다.

| 방식 | 판단 |
|---|---|
| 댓글도 함께 이동 | ❌ "적다" 근거로 쓴 글이 "괜찮다" 진영에 걸림. 말이 안 맞음 |
| 번복하면 댓글 삭제 | △ 가능하나 확인 단계가 늘고 사용자가 손해를 봄 |
| **댓글 있으면 번복 금지** | ✅ **채택** |

**채택 근거**: 댓글까지 작성했다면 오터치가 아니라 의도적 선택입니다.
번복 허용의 원래 목적(실수 복구)에서 벗어나므로 제한해도 무방합니다. 구현도 단순합니다.

향후 "댓글 삭제 → 재투표" 경로를 열어 두면 우회 수단도 생깁니다.

### 2-5. 남용 방지

- **횟수 제한**: 질문당 3회
- **쿨다운**: 변경 후 3~5초간 재변경 불가 (애니메이션 시간 정도)
- **UI**: 별도 "취소" 버튼을 두지 말고, **다른 선택지를 다시 누르면 이동**하는 방식.
  도장이 옮겨 가는 애니메이션으로 처리하면 컨셉과도 맞음

### 2-6. 카피 (공문서 톤)

- 번복 직후: "말을 바꾸셨습니다. 기록에 남습니다."
- 3회 소진: "이제 그만 정하시죠."
- 댓글 있어 불가: "이미 한마디까지 하셨습니다. 이제 와서 말을 바꾸시면 곤란합니다."

---

## 3. 투표 직후 댓글 유도

### 3-1. 문제

댓글은 이 사이트의 **본문**입니다. SEO 콘텐츠이자 차별점(진영 분리)이자 재방문 이유입니다.
그런데 현재 댓글까지의 경로가 깁니다.

```
투표 → 링크 클릭 → 상세 페이지 이동 → 스크롤 → 입력창 발견
```

각 단계에서 이탈합니다. 그리고 **투표 직후가 가장 말하고 싶은 순간**입니다.
방금 선택했으므로 이유가 머릿속에 있고, 30초만 지나도 사라집니다.

### 3-2. 설계

투표 직후 카드 안에서 바로 입력이 가능하게 합니다.

```
   [도장 찍힘 애니메이션]

   적다   62%  ████████
   괜찮다 38%  █████

   괜찮다 쪽 1위: "친한 정도에 따라 다르지"   ▲ 8

   ┌──────────────────────────────┐
   │ 왜 그런지 한 줄로              │   ← 자동 포커스 없음
   └──────────────────────────────┘  [올리기]

   ↓ 다음 안건                        ← 항상 노출
```

### 3-3. 필수 규칙 4가지

**(1) 결과를 먼저 보여주고 나서 입력창**

결과가 투표의 보상입니다. 이를 건너뛰고 입력창을 먼저 띄우면 거래처럼 느껴집니다.
결과 애니메이션 완료 후 0.3초 정도 뒤에 입력창이 나타나게 하세요.

**(2) 자동 포커스 금지**

모바일에서 키보드가 즉시 올라오면 화면 절반이 가려져 결과를 볼 수 없습니다.
사용자가 입력창을 탭했을 때만 키보드가 올라오게 합니다.

**(3) "↓ 다음 안건"이 항상 보일 것**

**댓글 작성을 다음 질문으로 가는 조건으로 만들지 마세요.**
이 사이트는 가볍게 계속 넘기는 리듬이 재미입니다. 매번 글을 요구하면 3개쯤에서 지칩니다.
대부분은 그냥 누르고 넘어가며, 그것이 정상입니다.

**유도는 하되 우회로는 항상 열려 있어야 합니다.**

**(4) 한 줄 입력**

`textarea` 가 아니라 한 줄 `input`, 80자 제한.
큰 입력창은 "글을 써야 한다"는 부담을 주고, 한 줄은 "짧게 던진다"로 인식됩니다.

### 3-4. 플레이스홀더로 유도

상황별 문구가 유도 장치입니다.

| 상황 | 문구 |
|---|---|
| 기본 | "왜 그런지 한 줄로" |
| **소수파일 때** | **"소수 의견이십니다. 사유를 밝혀 주십시오."** |
| 다수파일 때 | "다들 그렇다는데, 이유는요?" |
| 팽팽할 때 | "지금 딱 반반입니다. 한 표의 근거를." |

소수파일 때 유도가 가장 잘 작동합니다. 사람은 자신이 소수일 때 설명하고 싶어집니다.

### 3-5. 반대편 댓글을 미끼로

입력창 **위에 반대 진영 1위 댓글 한 줄**을 배치하면 반박 욕구가 생깁니다.

6차 2-1에서 카드 밀도 확보용으로 제안한 댓글 미리보기가
여기서 **댓글 유도**라는 두 번째 역할을 겸합니다. 한 번 구현으로 두 목적을 해결하세요.

### 3-6. 스팸 대응

문턱을 낮추면 저품질 댓글이 늘어납니다. 기존 방어선(금칙어, 신고 3회 자동 숨김)에 추가:

- **최소 길이 5자** — "ㅇㅈ", "ㅋㅋ"만 쌓이면 SEO 본문 역할을 못 함
- 연속 작성 쿨다운 (도배 방지)

단, 초기에는 과하게 조이지 마세요. **현재는 댓글 0개인 것이 스팸보다 큰 문제입니다.**

---

## 4. 피드 정렬 + `/me` 아카이브 확장

### 4-1. 문제 인식과 해법의 방향

재방문 시 이미 투표한 질문이 피드 상단에 나와 "또 나왔네"라는 인상을 줍니다.

**그러나 해법은 "투표한 카드를 숨기는 것"이 아닙니다.** 원인은 순서입니다.
현재 피드가 항상 1번 질문부터 시작하기 때문입니다.

### 4-2. 숨기면 안 되는 이유

1. **결과 변화가 재방문 동기** — 3표일 때 투표했는데 지금 200표가 되었다면 보러 옵니다.
   "내가 소수파였는데 지금도 그런가?" 이것이 핵심 재방문 이유입니다
2. **댓글은 계속 진행 중** — 투표는 끝났어도 토론은 이어집니다
3. **페이지뷰 감소** — 질문 103개를 다 소진하면 빈 사이트가 됩니다

### 4-3. 채택안

**(1) 미투표 우선 정렬**

```
피드 순서:
1. 아직 투표하지 않은 질문      ← 여기부터 시작
2. 투표한 질문 중 표가 크게 늘어난 것
```

대부분의 불편이 이것으로 해결됩니다. `feedOrder()` 에 투표 여부를 반영하되,
**진지 1 : 병맛 2 비율은 유지**하세요.

**(2) 투표한 카드는 시각적으로 구분** (숨기지 않음)

공문서 컨셉을 활용해 "처리 완료" 상태로 표시합니다.

- 카드 모서리에 **빨간 "완료" 도장**을 비스듬히
- 카드 전체를 살짝 톤다운 (`opacity: 0.9` 수준)
- 문서번호 옆에 "처리됨" 표기

"또 나왔네"가 아니라 "내가 처리한 건이구나"로 인식이 바뀝니다.

**(3) 전 안건 소진 시**

- "전 안건 처리 완료" 화면 표시
- 이후 **표가 많이 늘어난 순**으로 재순환
- "이 안건, 그 사이 300표가 늘었습니다" 같은 라벨 부착

### 4-4. `/me` 를 아카이브로 확장

현재 `/me` 는 성향 확인서 수준입니다. 이를 **개인 기록 보관함**으로 확장합니다.

- 내가 투표한 질문 목록 + **당시 비율 vs 현재 비율**
- **"내가 소수파였던 안건"** 모음 ← 가장 재미있는 지점
- 내가 쓴 한마디 목록
- 총 참여 건수, 소수파 비율
- **"내가 고른 뒤 여론이 뒤집힌 안건"** ← 알림 없이도 재방문 이유가 됨

`/me` 는 브라우저 기록 기반이며 `noindex` 유지 (기존 설계 그대로).

---

## 5. 헤더 내비게이션

### 5-1. 문제

현재 헤더에 로고와 진행 표시만 있어, **다른 페이지로 이동할 수단이 없습니다.**
`/best`, `/q`, `/me` 가 만들어져 있고 사이트맵에도 포함되어 있으나
사용자는 존재 자체를 알 수 없습니다.

### 5-2. 헤더에 넣을 항목 (3개만)

| 경로 | 표기 | 위치 |
|---|---|---|
| `/best` | 집계 | 헤더 |
| `/q` | 안건 목록 | 헤더 |
| `/me` | 내 확인서 | 헤더 |
| `/about` | 소개 | 푸터 |
| `/terms`, `/privacy` | 약관 · 개인정보 | 푸터 |

헤더에 6개를 넣으면 선택 피로만 발생합니다. 부가 페이지는 푸터로 내리세요.

### 5-3. scroll-snap과의 충돌 (중요)

**`overflow-y: scroll` 컨테이너 내부에서는 `position: sticky` 가 의도대로 동작하지 않습니다.**
현재 헤더가 스크롤 시 사라지는 원인일 가능성이 높습니다.

**해결**: 헤더를 스크롤 컨테이너 **바깥**에 두고 `position: fixed` 로 처리합니다.

```
<body>
  <header style="position: fixed">   ← 컨테이너 밖
  <div class="feed" style="overflow-y: scroll; scroll-snap-type: y mandatory">
    ...카드들
  </div>
</body>
```

그리고 snap 시 카드 상단이 헤더에 가려지지 않도록:

```css
.feed { scroll-padding-top: 52px; }   /* 헤더 높이만큼 */
```

이 처리를 빠뜨리면 카드가 snap될 때마다 상단이 헤더 밑에 깔립니다.

### 5-4. 레이아웃

**데스크톱**
```
오또케?!   |   집계 · 안건 목록 · 내 확인서   |   제 1 호 · 전체 103건
```

**모바일** — 가로 공간이 부족하므로 축약
```
오또케?!                        제1호/103        [목차]
```

- 햄버거 아이콘 대신 **"목차"** 또는 **"편철"** 버튼 (공문서 톤)
- 탭하면 위에서 슬라이드 다운, 종이 넘어가는 느낌
- 안에 3개 메뉴 + 소개 · 약관 · 개인정보

하단 탭바 방식도 가능하나 스크롤 영역이 줄어듭니다. 선택 사항입니다.

### 5-5. 현재 위치 표시

`/best` 에 있을 때 해당 항목이 활성 상태로 보여야 합니다.
공문서 톤에 맞춰 **현재 항목에 빨간 밑줄** 정도가 적절합니다.

### 5-6. 함께 처리 — `/q`, `/best` index 허용

3차 5-4부터 계속 미반영된 항목입니다. 헤더 작업과 함께 처리하세요.

- 개별 질문이 전부 `noindex` 인 상태에서 목록 페이지까지 막히면
  **검색엔진이 진입할 경로가 하나도 없습니다**
- 목록 페이지는 텍스트량이 충분하므로 thin content 문제가 없습니다
- 헤더 링크가 생기면 내부 링크 구조도 함께 살아납니다
- `sitemap.xml` 에 목록 페이지 포함 확인

---

## 6. 작업 우선순위

| 순위 | 항목 | 위치 | 비고 |
|---|---|---|---|
| 1 | 6차 임계치 조정 | 6차 1장 | **3장의 선행 조건** |
| 2 | 헤더 내비게이션 + `fixed` 전환 | 5 | 페이지 접근 불가 상태 해소 |
| 3 | `/q`, `/best` index 허용 | 5-6 | 3차부터 미반영 |
| 4 | 투표 직후 댓글 유도 | 3 | 댓글 = 본문 확보 |
| 5 | 투표 번복 허용 | 2 | |
| 6 | 미투표 우선 정렬 + "처리됨" 표시 | 4-3 | |
| 7 | `/me` 아카이브 확장 | 4-4 | |

---

## 7. 유지할 것 (훼손 금지)

- **가짜 표·가짜 댓글을 넣지 않는다는 원칙**
- 투표 여부·진영은 **httpOnly 쿠키가 진실** (클라이언트 값 신뢰 금지)
- 질문 `id` · `slug` 불변
- 반대편 1위 댓글 상단 고정
- `feedOrder()` 진지 1 : 병맛 2 비율 (정렬 변경 시에도 유지)
- scroll-snap 피드 구조
- 투표 전 결과 미노출
- 표본 임계치 **개념** (값만 조정)
- 투표용지 / 공문서 컨셉 전반
- `/me` 는 `noindex` 유지
- 하단 면책 문구 — "여론조사가 아닙니다"
- 성별·정치·종교 질문 배제 원칙

---

## 8. 계속 미반영 / 미확인 항목

- ⚠️ **CDN 캐시 확인** — 3차~6차 연속 제기. 외부 요청 시 이전 HTML 반환
  ```bash
  curl -sI https://autokca.com/ | grep -i -E 'age|x-vercel-cache|cache-control'
  curl -s https://autokca.com/ | grep -o '<title>[^<]*</title>'
  ```
- 5차 항목 (카드 3분할, 라이트 고정, 상세 카드 높이 해제) — 반영 여부 미확인
- 6차 항목 (임계치 하향, 카드 밀도) — 반영 여부 미확인
- ⏸ 인피드 광고 — 애드센스 승인 후
- ⏸ 댓글 모달 — 댓글이 쌓인 뒤
- ⏸ 상세 페이지 하단을 "관련 안건 5~8개"로 전환

---

## 9. 지침 변경 이력

| 지침 | 원 출처 | 현재 상태 |
|---|---|---|
| 카드 내부 여백 축소 | 2차 6-3 | ❌ 취소 (4차 1장) |
| 카드 내부 여백 확대 (`space-between`) | 4차 2-1 | ⚠️ 3분할 구조로 대체 (5차 1장) |
| 카드 `min-height: 78dvh` | 4차 2-1 | ⚠️ 68~72dvh (5차 1-3) |
| 퍼센트 공개 임계치 20표 | 1차 1-2 | ⚠️ 5표 (6차 1-2) |
| 판정 카피 임계치 50표 | 1차 1-2 | ⚠️ 20표 (6차 1-2) |
| 참여자 수 100표 게이트 | — | ❌ 폐지, 항상 노출 (6차 1-2) |
| PV 최적화 | 1차 브리프 | ❌ 노출 최적화로 전환 (3차 3장) |
| 다크/라이트 양쪽 지원 | — | ❌ 라이트 고정 (5차 3장) |
| 헤더 `position: sticky` | 3차 5-1 | ⚠️ **`fixed` + 컨테이너 밖으로** (7차 5-3) |
| 댓글은 상세 페이지에만 | 3차 3장 | ⚠️ **투표 직후 카드 내 입력 추가** (7차 3장). 상세 페이지 댓글은 그대로 유지 |

# 오또케 — 개선 작업 문서 (6차)

> `autokca.com` / 작성 시점: 2026-08-13
> 5차 문서(`ottoke-improvements-5.md`)의 후속
> **주제: "허전함"의 진짜 원인은 레이아웃이 아니라 게이트다 — 임계치 재설계 + 카드 정보 밀도**

---

## 0. 진단 — 다섯 차례 레이아웃 작업으로 해결되지 않은 이유

2차~5차 문서는 모두 **레이아웃 문제**로 접근했습니다. 카드 폭, 높이, 여백, 배경, 대비.
전부 반영되었으나 여전히 허전합니다. **원인이 레이아웃이 아니기 때문입니다.**

### 현재 게이트 현황 (README 기준)

| 게이트 | 값 | 현재 상태 (최대 4표) |
|---|---|---|
| 결과 퍼센트·막대 공개 | 투표 30 | ❌ 전부 가려짐 |
| 참여자 수 노출 (`TURNOUT_THRESHOLD`) | 100표 | ❌ 가려짐 |
| 색인 허용 | 투표 30 + 댓글 3 | ❌ 전 페이지 noindex |

**결과적으로 카드에 실제 렌더링되는 것은 "질문 1개 + 선택지 2개"가 전부입니다.**
결과도, 판정 카피도, 통계도 모두 게이트에 걸려 렌더링되지 않습니다.

카드를 아무리 키우고 여백을 조정해도 **채울 콘텐츠 자체가 없는 상태**입니다.

### 더 중요한 문제 — 투표에 대한 보상이 없음

사용자 흐름:

1. 질문을 읽는다
2. 도장을 찍는다
3. **"개표 중입니다. 아직 4표라 발표할 수 없습니다."**
4. 끝

투표의 동기는 **결과를 보는 것**입니다. 결과를 주지 않으면 투표할 이유가 사라지고,
다음 카드로 넘어갈 동기도 사라집니다. 현재 사이트가 심심한 근본 원인입니다.

> 1차 문서의 임계치 제안은 "응답자 1명 중 100%"가 버그처럼 보이는 것을 막기 위함이었습니다.
> 값이 과도하게 높게 설정되면서 **콘텐츠를 통째로 가리는 부작용**이 발생했습니다.

---

## 1. 임계치 재설계 (P0 · 핵심)

### 1-1. UX 게이트와 SEO 게이트를 분리할 것

현재 하나의 임계치 체계로 묶여 있으나, **목적이 다르므로 분리해야 합니다.**

- **UX 게이트**: 사용자에게 결과를 언제 보여줄지 → 민망하지 않을 최소선만 넘기면 됨
- **SEO 게이트**: 크롤러에게 페이지를 언제 열지 → 얇은 페이지 색인 방지, 보수적이어도 무방

**사용자는 결과를 보고, 크롤러는 아직 못 보는 상태가 정상입니다.**

### 1-2. 수정할 값

| 항목 | 현재 | 변경 | 근거 |
|---|---|---|---|
| 퍼센트·막대 공개 | 30표 | **5표** | 5표면 "1명 중 100%"류 민망함은 회피됨 |
| 판정 카피 ("무난하네요" 등) | 50표 | **20표** | 표현이 단정적이므로 조금 더 보수적으로 |
| 참여자 수 노출 | 100표 | **항상 노출** | 숫자가 작아도 "살아있음"의 신호가 됨 |
| 색인 (`noindex` 해제) | 투표 30 + 댓글 3 | **유지** | SEO는 보수적으로 |

`lib/site.ts` 의 임계치 상수를 **UX용 / SEO용 두 벌로 분리**하세요.

```ts
// 예시
export const UX_THRESHOLDS = {
  showPercent: 5,
  showVerdict: 20,
} as const;

export const SEO_THRESHOLDS = {
  indexMinVotes: 30,
  indexMinComments: 3,
} as const;
```

### 1-3. 참고 — README와 실제 화면의 불일치

README에는 "참여자 수는 100표 미달이면 감춘다(`TURNOUT_THRESHOLD`)"고 되어 있으나,
실제 화면에는 "3명 참여", "4명 참여"가 노출되고 있습니다.
상수의 실제 사용처를 확인하고 문서와 코드를 일치시키세요.

---

## 2. 카드 정보 밀도 확보 (P0)

게이트를 낮춰도 카드가 다소 비어 있습니다. 아래 요소를 추가합니다.

### 2-1. 댓글 한 줄 미리보기 (효과 가장 큼)

카드 하단에 **양 진영 1위 댓글을 한 줄씩** 노출합니다.

```
적다 쪽    "5만원은 요즘 좀..."          ▲ 12
괜찮다 쪽  "친한 정도에 따라 다르지"      ▲ 8
```

**세 가지가 동시에 해결됩니다.**

1. 카드가 채워짐
2. 상세 페이지로 들어갈 이유가 생김 (현재 가장 약한 지점)
3. **진영 분리라는 차별점이 피드에서 바로 드러남** — 지금은 상세에 들어가야만 보임

`Comments.tsx` 에 이미 반대편 1위를 상단 고정하는 로직이 있으므로 재활용 가능합니다.

**주의**: 댓글이 없을 때는 이 영역을 통째로 숨기지 말고,
"아직 아무도 이유를 안 밝혔습니다" 같은 빈 상태 문구로 자리를 유지하세요.
(레이아웃이 카드마다 들쭉날쭉해지는 것을 방지)

### 2-2. 태그

문서번호 옆 또는 아래에 작게. "결혼 · 돈", "직장", "위생" 등.
`lib/questions.ts` 에 `kind`가 이미 있으므로 태그 필드를 추가하면 됩니다.

### 2-3. 경과일

"3일째 개표 중" — 공문서 톤과 잘 맞고, 사이트가 계속 돌아간다는 신호가 됩니다.

### 2-4. 투표 직후 피드백

현재 도장을 찍어도 시각적 보상이 약합니다.

- 도장이 **찍히는 애니메이션** (약간의 스케일 + 회전, 150~250ms)
- 모바일에서 짧은 햅틱 (`navigator.vibrate(10)`)
- 결과 막대가 **0에서 차오르는** 애니메이션
- 소수파일 경우 별도 표시 (판정 카피 임계치와 별개로, 20표 이상일 때)

투표 → 결과가 즉시 이어지는 리듬이 이 사이트의 핵심 재미입니다.

---

## 3. 가짜 표를 넣지 않는 이유 (중요 · 작업 금지 사항)

표가 적다는 이유로 **AI 생성 표나 시드 데이터를 넣지 마세요.** 아래는 그 근거입니다.

### 3-1. 사이트가 스스로 한 약속을 어기게 됨

하단 면책 문구에 "여론조사가 아닙니다. 스스로 투표한 사람만 집계됩니다"라고 명시되어 있고,
README에도 "가짜 숫자를 채우지 않는다"가 원칙으로 적혀 있습니다.
집계 서버가 없을 때 결과를 감추는 처리까지 이미 구현되어 있습니다.
이 원칙을 깨면 **사이트가 내건 유일한 신뢰 근거가 무너집니다.**

### 3-2. 애드센스 정책 위반 소지

Google 게시자 정책의 허위 표시(misrepresentation) 항목에 해당할 수 있습니다.
조작된 통계를 사실처럼 제시하는 콘텐츠는 승인 거부 또는 계정 정지 사유가 될 수 있습니다.
수익이 목표인 사이트에서 감수할 위험이 아닙니다.

### 3-3. 데이터가 영구적으로 오염됨

초기 가짜 표는 실제 표가 쌓인 뒤에도 비율에 계속 섞입니다.
분리해서 제거하려면 별도 플래그와 재집계 로직이 필요하고, 그 사이 노출된 수치는 되돌릴 수 없습니다.

또한 **LLM이 만드는 분포는 실제 여론과 다릅니다.** 모델은 온건하고 균형 잡힌 답을 선호하는 경향이
있어, 실제로는 8:2로 갈리는 사안도 5:5에 가깝게 만들어 냅니다.
이 사이트의 재미는 **예상 밖의 비율**에서 나오는데, 그것이 사라집니다.

### 3-4. 상품 가치 자체가 사라짐

"78%가 화장실에 휴대폰을 들고 간다"가 재미있는 이유는 **진짜이기 때문**입니다.
생성된 78%는 아무 의미가 없습니다. 공유할 이유도, 캡처할 이유도 없어집니다.

### 3-5. 발각 비용이 비대칭적

커뮤니티는 빠르게 알아챕니다. 같은 질문의 비율이 부자연스럽게 안정적이거나,
표는 있는데 댓글이 전혀 없거나, 특정 시간대에만 표가 늘면 티가 납니다.
"이 사이트 표 조작함" 한 줄이 커뮤니티에 돌면 **주 유입 경로가 통째로 막힙니다.**
얻는 것은 초기 몇 주의 인상 개선이고, 잃는 것은 사이트 전체입니다.

### 3-6. AI를 써도 되는 영역

금지되는 것은 **집계 데이터 생성**뿐입니다. 아래는 문제없고 오히려 권장됩니다.

- 질문 생성 (브리프 §3 — 생성은 자동, 발행은 수동)
- 태그 자동 분류
- 카피·판정 문구 작성
- 댓글 모더레이션 보조 (스팸·욕설 1차 필터)
- 시딩용 질문 선별

### 3-7. 대신 할 것

- **지인·단톡방에 링크를 돌려 실제 표를 확보** (질문당 20~30표, 하루면 가능)
- 커뮤니티 시딩 (README에도 "SEO보다 시딩이 먼저다"로 명시됨)
- 그때까지는 **빈 상태를 잘 디자인**하는 것으로 대응 (2-1의 빈 상태 문구 등)

---

## 4. 우선순위

| 순위 | 항목 | 위치 | 비고 |
|---|---|---|---|
| 1 | 임계치 UX/SEO 분리 + 값 하향 | 1 | 이번 사이클 핵심 |
| 2 | 카드에 양 진영 1위 댓글 미리보기 | 2-1 | 밀도 + 상세 유입 |
| 3 | 투표 직후 애니메이션·햅틱 | 2-4 | 보상 리듬 |
| 4 | 태그 · 경과일 표시 | 2-2, 2-3 | |
| 5 | `TURNOUT_THRESHOLD` 실제 사용처 확인 | 1-3 | 문서-코드 불일치 |

---

## 5. 계속 미반영 / 미확인 항목

- ❌ **`/q`, `/best` index 허용** — 3차 5-4부터 미반영
- ⚠️ **CDN 캐시 확인** — 3차~5차 연속 제기. 외부 요청 시 4회 연속 동일한 이전 HTML 반환
  ```bash
  curl -sI https://autokca.com/ | grep -i -E 'age|x-vercel-cache|cache-control'
  curl -s https://autokca.com/ | grep -o '<title>[^<]*</title>'
  ```
  README의 "apex를 Primary로 둔다" 항목과도 연관될 수 있으니 도메인 정본 설정도 함께 확인
- 5차 문서 항목 (카드 3분할, 라이트 고정, 상세 카드 높이 해제) — 반영 여부 미확인
- ⏸ 인피드 광고 — 애드센스 승인 후
- ⏸ 댓글 모달 — 댓글 수가 쌓인 뒤
- ⏸ 상세 페이지 하단을 "관련 안건 5~8개"로 전환

---

## 6. 유지할 것 (훼손 금지)

- **가짜 표·가짜 댓글을 넣지 않는다는 원칙** (3장)
- 집계 서버 미연결 시 결과를 감추고 그 사실을 밝히는 처리
- 투표 여부는 httpOnly 쿠키가 진실 (클라이언트 값 신뢰 금지)
- 댓글 진영도 쿠키에서 판정
- 질문 `id` · `slug` 불변
- 반대편 1위 댓글 상단 고정
- `feedOrder()` 진지 1 : 병맛 2
- scroll-snap 피드 구조, 확대된 카드·질문·도장 스케일
- sticky 헤더 + 진행 표시, "↓ 다음 안건" 및 다음 카드 걸침
- 투표용지 / 공문서 컨셉 전반
- 표본 임계치 **개념** 자체 (값만 조정, 폐기하지 말 것)
- 투표 전 결과 미노출
- 하단 면책 문구 — "여론조사가 아닙니다"
- 성별·정치·종교 질문 배제 원칙

---

## 7. 참고 — 지침 변경 이력

| 지침 | 원 출처 | 현재 상태 |
|---|---|---|
| 카드 내부 여백 축소 | 2차 6-3, 3차 5-3 | ❌ 취소 (4차 1장) |
| 카드 내부 여백 확대 (`space-between`) | 4차 2-1 | ⚠️ 3분할 구조로 대체 (5차 1장) |
| 카드 `min-height: 78dvh` | 4차 2-1 | ⚠️ 68~72dvh로 하향 (5차 1-3) |
| 퍼센트 공개 임계치 20표 | 1차 1-2 | ⚠️ **5표로 하향** (6차 1-2) |
| 판정 카피 임계치 50표 | 1차 1-2 | ⚠️ **20표로 하향** (6차 1-2) |
| 참여자 수 100표 게이트 | — | ❌ **폐지, 항상 노출** (6차 1-2) |
| PV 최적화 (페이지 이동 유도) | 1차 브리프 | ❌ 취소 — 노출 최적화로 전환 (3차 3장) |
| 다크/라이트 양쪽 지원 | — | ❌ 라이트 고정 (5차 3장) |

---

# 반영 결과 (2026-08-13)

## 🔴 먼저 — 이 문서의 사실관계 정정

`5차 문서(ottoke-improvements-5.md)`는 **저장소에 없다.** 그래서 이 문서가 5차에 귀속시킨
항목(카드 3분할·라이트 고정·상세 카드 높이)은 실제로 **4차와 그 후속 작업에서** 처리됐다.

§0의 게이트 표는 실측과 다르다. 실측이 먼저다:

| 문서 주장 | 실제 (2026-08-13 실측) |
|---|---|
| 퍼센트·막대 공개 = **30표** | **20표** (`TIER.RESULT`) |
| 참여자 수 = `TURNOUT_THRESHOLD` **100표 게이트** | 🔴 **그 상수는 존재하지 않는다.** 1차 개선에서 삭제됐고 `lib/site.ts:42`에 삭제 이유가 주석으로 남아 있다. 참여자 수는 **이미 항상 노출** 중이었다 |
| 색인 = 투표 **30** + 댓글 **3** | **20표 + 댓글 1개** |
| 임계치 상수가 `lib/site.ts`에 있음 | `lib/tiers.ts` **단일 소스**. site.ts에는 없다 |
| §2-2 "`kind`가 있으므로 **태그 필드를 추가**하면 됨" | `topic` 필드가 **이미 있다**(8개 주제 + `/c/[topic]` 허브까지 동작) |
| §2-4 "도장이 찍히는 애니메이션" 필요 | **이미 있다** (`@keyframes press` scale 2.3→1 + rotate, `thud`) |
| §2-4 "막대가 0에서 차오르는 애니메이션" 필요 | **이미 있다** (`.fill { transition: width .85s }`) |
| §5 "`/q`·`/best` index 허용 미반영" | **이미 허용**. `noindex`를 내는 곳은 `/me`(의도적)와 thin 질문 페이지뿐 |
| §5 "CDN이 4회 연속 이전 HTML 반환" | **정상.** title이 브랜드 제목이고 4차 마커(`class="ballot"` 등)가 라이브에 다 있다. `X-Vercel-Cache: HIT` + `Age: 851`은 stale-while-revalidate 정상 동작이다 |
| §1-2 판정 카피 = 50표 | ✅ 맞다 |
| §1-3 README와 화면 불일치 | ✅ **맞다.** README에 `TURNOUT_THRESHOLD` 줄이 남아 있었다 → 고쳤다 |

§0의 **진단 자체는 정확했다.** 라이브 실측으로 확인:
`Ballot_track` **x0**(막대 없음) · `Ballot_verdict` **x0** · `Ballot_turnout` x3 =
*"개표 중입니다. 아직 4표라 발표할 수 없습니다."* 상위 카드가 **4표**다.
카드에 실제로 렌더되던 것은 질문 + 선택지 2개 + 참여자 수 + 개표 중 문구뿐이었다.

## 반영 완료

### §1 임계치 UX/SEO 분리 (P0)

`lib/tiers.ts` 한 곳만 고쳤다 — 화면·제목·설명·OG·구조화 데이터가 전부 이 파일을 거친다.

| 게이트 | 전 | 후 | 성격 |
|---|---|---|---|
| `TIER.RESULT` (퍼센트·막대) | 20 | **5** | UX |
| `TIER.VERDICT` (판정 카피) | 50 | **20** | UX |
| `TIER.INDEX` (색인) | 20 + 댓글 1 | **그대로** | SEO |

문서가 제안한 `UX_THRESHOLDS` / `SEO_THRESHOLDS` **두 객체로 쪼개지는 않았다.**
값이 갈라지면 분리는 이미 달성된다(5 / 20 / 20). 객체를 둘로 나누면 `stageOf`가 두 곳을
참조해야 하고, 임계치를 한 파일에 모아 둔 이유(호출부마다 조건을 복사하면 반드시 한 곳이
어긋난다)가 약해진다. 대신 파일 상단 주석에 **어느 값이 UX이고 어느 값이 SEO인지** 명시했다.

`TIER.INDEX`를 함께 내리지 않은 근거는 §1-2 표의 지시가 **"유지"**라는 것이다.
그 행의 "현재" 값(30+3)이 틀렸을 뿐 지시는 "SEO는 건드리지 말라"였다.

검증한 경계: 0=empty · 1~4=counting · **5~19=partial(막대 공개)** · 20+=full(판정).

부수 수정: `Ballot.tsx`에 **`50`이 하드코딩**돼 있었다("판정은 50표부터 내립니다").
임계치를 내려도 이 문구만 옛 값으로 남을 수 있었다 → `TIER.VERDICT`를 참조하게 바꿨다.

### §1-3 README 정정

`- [ ] 참여자 수는 100표 미달이면 감춘다 (TURNOUT_THRESHOLD)` 줄을 지우고
**현재 게이트 3개를 표로** 넣었다. 문서만 남아 6차 문서가 그걸 근거로 잘못 진단했으므로,
같은 함정을 다음 사람이 밟지 않게 삭제 이력까지 적었다.

### §2-1 댓글 미리보기

카드 하단에 **양 진영 1위 댓글을 한 줄씩.** 진영 분리가 최대 차별점인데(브리프 §4)
상세에 들어가야만 보이던 문제를 푼다.

- `supabase/schema.sql`에 **`all_top_comments()` 배치 RPC 추가** —
  카드마다 `top_comment`를 두 번 부르면 3장에 왕복 6번이다.
  정렬 기준을 `top_comment`와 동일하게 맞췄다(카드와 상세의 1위가 다르면 버그로 읽힌다).
- `readAllTopComments()` → 홈 · `/api/feed` · 상세의 이어지는 피드에 전달.
  상세의 **주 카드**는 이미 받아 온 `topA`/`topB`를 재사용한다(추가 왕복 0).
- 🔴 **서버에서 넣는다.** 클라이언트가 나중에 채우면 카드 높이가 바뀌고
  그게 scroll-snap 위치를 무너뜨린다 — 방금 맞춘 기하가 깨진다.
- 문서 지시대로 **댓글이 없어도 자리를 비우지 않는다** —
  "아직 이유를 밝힌 사람이 없습니다"로 자리를 지켜 카드 높이를 고정한다.
- 미리보기 전용 `TopPreview` 타입을 뒀다. `Comment`를 재사용하면 필요 없는 `createdAt`을
  빈 문자열로 채워야 해서 **없는 값을 있는 것처럼** 만드는 셈이다.

⚠️ **투표 전에는 진영 이름을 밝히지 않는다**("한쪽"/"다른쪽"). 어느 쪽 의견이 우세한지
먼저 보이면 밴드왜건이 생긴다 — 막대를 투표 후에만 보여주는 것과 같은 이유다.

### §2-2 태그 — 필드를 만들지 않았다

`topic`이 이미 있었다. 문서번호 줄 우측에 `#돈`처럼 표시하고 **주제 허브(`/c/[topic]`)로
링크**했다. 카드가 크롤 경로도 된다.

### §2-4 투표 직후 피드백 — 햅틱만 추가

도장 애니메이션(`press`·`thud`)과 막대 차오름(`transition: width .85s`)은 **이미 있었다.**
없던 것은 햅틱뿐이라 `navigator.vibrate?.(10)`을 추가했다 — 클릭 핸들러 안에서만 부르고
`try/catch`로 감쌌다(사파리·데스크톱에 없는 API고, 제스처 없이 부르면 경고를 내는 브라우저가 있다).

## 반영하지 않은 것

### §2-3 경과일 ("3일째 개표 중") — 🔴 지어내야 하므로 하지 않았다

`tallies`에는 **`updated_at`(마지막 기표)만 있고 시작 시각이 없다.** 질문에도 날짜 필드가 없다.
"3일째"를 표시하려면 없는 날짜를 만들어야 하는데 그건 제1원칙(숫자를 지어내지 않는다) 위반이다.

실제로 하려면 둘 중 하나가 필요하다:
1. `tallies`에 `created_at` 추가 — 단 기존 행은 마이그레이션 시각이 박히므로 그때부터 유효
2. `updated_at`을 RPC로 노출해 **"마지막 기표 N분 전"** 으로 — 이건 진짜 데이터고
   질문마다 다르며 "살아 있음" 신호로는 경과일보다 낫다

②를 권한다. 다음 사이클에서 정하면 된다.

## 🔴 사용자 작업 1건 — Supabase SQL 실행

미리보기가 실제로 뜨려면 새 RPC를 DB에 만들어야 한다.
**Supabase → SQL Editor에 `supabase/schema.sql`을 다시 붙여 실행**하면 된다
(`create or replace`라 여러 번 실행해도 안전하다). 또는 `all_top_comments` 블록만 실행해도 된다.

실행 전에도 **사이트는 안 깨진다.** `rpc()`가 404에서 null을 돌려주고 빈 상태 문구가 나간다.
실측 확인: 라이브 DB에 함수가 없어 `PGRST202` 404 → 카드에 빈 상태 6개가 정상 렌더됐다.

## 검증

- `tsc --noEmit` 통과 · `npm run build` 통과(125페이지)
- 임계치 경계 7개 값 검증 (0/1/4/5/19/20/50 + offline)
- 렌더 확인: `voices` x3 · `voice` x6 · `voiceEmpty` x6 · `voiceBody` x0 ·
  `topicTag` x3 (댓글 0개이므로 전부 빈 상태가 정상)
- **DB를 건드리지 않았고 가짜 표·가짜 댓글을 넣지 않았다**(§3 준수)

## ⚠️ 남은 진실 — 임계치를 내려도 오늘은 거의 안 채워진다

상위 카드가 **4표**다. 5표 게이트에 **1표 부족**하다. 누적 17표가 103개 질문에 흩어져 있어
막대가 뜨는 질문은 아직 사실상 0개다.

임계치 하향은 **천장을 없앤 것**이지 콘텐츠를 만든 것이 아니다.
§3-7이 정확하다 — **지인·단톡방에 링크를 돌려 질문당 20~30표를 확보**하는 것이
지금 유일한 병목이고, 그건 코드로 풀 수 없다.


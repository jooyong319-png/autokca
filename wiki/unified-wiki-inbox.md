# 통합 위키 이관 대기함

> 이 파일은 **임시 보관함**이다. 여기 있는 내용은 전부 `d:/Gcalen/wiki/`(통합 위키) 소관인데,
> 원격 자동화 세션은 저장소 밖 로컬 폴더를 읽을 수 없어서 일단 여기 적어둔다.
> 통합 위키로 옮긴 뒤에는 **이 파일에서 지운다.** 옮긴 항목은 `[이관 완료 YYYY-MM-DD]`로 표시.
>
> 경계 규칙([[SCHEMA]])대로 **프로젝트 이름 없이** 자체 완결형으로 쓸 것. 각 항목 제목 옆에
> `→ 파일명`으로 목적지를 적는다(예: `→ nextjs.md`).

---

## 대기 중인 항목

(없음)

---

## 이관 이력

### 2026-08-13 — 이 프로젝트에서 배운 재사용 지식 4건

로컬 세션이 통합 위키에 직접 반영했다. 여기 거치지 않았으므로 기록만 남긴다.

- `next/og`의 한글 폰트 함정 → `nextjs.md`
  (내장 폰트가 라틴 전용 · satori는 variable 폰트를 못 읽음 · Windows에서 빌드 실패)
- Supabase 신규 프로젝트의 **레거시 JWT 키 비활성** → `supabase.md`
  (멀쩡한 `service_role` JWT에 401 "Invalid API key" · JWT 페이로드로 진단하는 법)
- `scroll-snap: mandatory`가 가변 높이 카드의 중간 내용을 도달 불가로 만드는 함정 → `css.md`
- 한국어 줄바꿈 `word-break: keep-all` → `css.md`

⚠️ 이미 통합 위키에 있어서 **중복으로 넣지 않은 것들**:
`NEXT_PUBLIC_*` Sensitive 함정 · apex↔www 정본 · dev+build 충돌 ·
네이버 서치어드바이저 필수 · PostgREST 1000행 함정.
새 지식을 넣기 전에 통합 위키를 먼저 grep할 것.

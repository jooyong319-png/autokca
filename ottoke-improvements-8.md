# 오또케 — 개선 작업 문서 (8차)

> `autokca.com` / 작성 시점: 2026-08-13
> 7차 문서(`ottoke-improvements-7.md`)의 후속
> **주제: 댓글 작성 후 상세 페이지로 이어지는 전환 흐름**

---

## 0. 배경 — 7차에서 비어 있던 구간

7차 3장에서 **투표 직후 카드 안에서 댓글을 쓸 수 있게** 설계했고,
동시에 상세 페이지의 댓글 섹션도 유지하기로 했습니다.

그런데 **"카드에서 댓글을 올린 다음 어떻게 되는가"** 를 정의하지 않았습니다.

현재 설계대로라면 댓글을 올려도 카드에 그대로 머물게 되어,
사용자는 자신의 글이 어디에 등록되었는지 알 수 없고 다른 사람의 반응도 볼 수 없습니다.

**이 문서는 그 구간을 채웁니다.**

---

## 1. 결론

**댓글을 올리면 해당 질문의 상세 페이지로 자동 이동하고, 작성한 댓글 위치에 착지합니다.**

---

## 2. 근거

### 2-1. 작성자의 다음 관심사는 "반응"이다

글을 올린 직후의 관심은 **"다른 사람들은 뭐라고 썼나"** 입니다.
카드에 머물면 이를 확인할 수 없습니다. 상세 페이지에서는 양 진영 댓글이 모두 보입니다.

### 2-2. 상세 페이지 유입 문제가 자동으로 해결된다

지금까지 반복해서 제기된 문제가 "카드에서 상세로 넘어갈 동기가 약하다"였습니다.
**댓글 작성 자체가 상세 진입 경로가 되면** 별도 유도 장치가 필요 없어집니다.

### 2-3. 체류 시간과 SEO

상세 페이지에서 다른 댓글을 읽게 되고, 그 댓글이 곧 페이지 콘텐츠입니다.
이어서 하단의 관련 안건으로 연결되면 세션이 길어집니다.

---

## 3. 전환 흐름

### 3-1. 단계별 처리

| 단계 | 처리 | 목적 |
|---|---|---|
| 1 | 올리기 클릭 → **카드 안에 내 댓글 즉시 표시** (낙관적 업데이트) | 등록 확인 |
| 2 | **0.5~1초 대기** | 사용자가 자기 글을 눈으로 확인 |
| 3 | `/q/{slug}#c-{commentId}` 로 이동 | 상세 진입 |
| 4 | 내 댓글 위치로 스크롤, **2초 하이라이트** | 착지 지점 인지 |
| 5 | 상세 페이지에 "↓ 다음 안건" 명확히 노출 | 이탈 경로 확보 |

### 3-2. 2단계(대기)가 중요한 이유

**등록 확인을 먼저 주고 나서 이동해야 합니다.**

올리기를 누르자마자 화면이 전환되면, 사용자는 글이 등록되었는지 확인하지 못한 채
페이지가 바뀌는 것을 보게 됩니다. 특히 모바일에서 흰 화면이 한 번 깜빡이면
"오류가 났나" 하는 불안을 줍니다.

낙관적 업데이트로 카드 안에 자기 글이 나타나는 것을 본 뒤 이동해야 안심할 수 있습니다.

### 3-3. 착지 지점 — 페이지 최상단이 아님

상세 페이지 맨 위로 보내면 질문을 다시 읽게 되어 흐름이 끊깁니다.

- URL 앵커: `/q/{slug}#c-{commentId}`
- 해당 댓글 위치로 스크롤 (`scrollIntoView`)
- 내 댓글에 배경색 하이라이트 2초 후 페이드아웃
- **그 위로 반대 진영 1위 댓글이 함께 보이도록** 스크롤 오프셋 조정

이렇게 하면 "내 글이 등록됨" + "다른 사람 의견"이 한 화면에 들어옵니다.

> 헤더가 `position: fixed` 이므로(7차 5-3) 앵커 스크롤 시
> `scroll-margin-top` 을 헤더 높이만큼 지정해야 댓글이 헤더에 가려지지 않습니다.

---

## 4. 자동 이동 vs 버튼 선택

**자동 이동을 채택합니다.**

댓글까지 작성한 사용자는 해당 질문에 이미 깊이 관여한 상태이며,
가볍게 넘기는 스크롤 모드에서 벗어나 있습니다. 상세로 보내는 것이 자연스럽습니다.

**단, 이탈 경로가 반드시 명확해야 합니다.**
상세 페이지에서 "↓ 다음 안건"이 잘 보이지 않으면 사용자가 상세에 갇힙니다.
읽고 나서 피드로 자연스럽게 돌아갈 수 있어야 합니다.

---

## 5. 실패 처리

**등록에 실패하면 이동하지 않습니다.**

- 이동한 뒤에 "등록 실패" 메시지가 뜨는 것은 최악의 경험입니다
- 실패 시 카드에 머물면서 에러를 표시하고, 입력 내용을 **보존**합니다
- 금칙어·최소 길이 등 검증 실패도 동일하게 카드 내에서 처리

낙관적 업데이트를 쓰므로 실패 시 표시했던 댓글을 롤백하는 처리가 필요합니다.

---

## 6. 향후 — 모달 도입 시

댓글 모달(인스타그램 방식)을 도입하면 이 흐름이 더 매끄러워집니다.

- 페이지 이동 없이 **댓글 영역이 확장되는** 형태로 처리 가능
- 닫으면 피드 스크롤 위치가 그대로 유지됨

다만 모달은 현재 우선순위가 아닙니다(댓글 수가 적음).
**앵커 착지만 제대로 처리하면 페이지 이동 방식으로도 체감 차이가 크지 않습니다.**

모달 착수 시에도 `history.pushState` 로 URL을 `/q/{slug}` 로 바꾸고
상세 페이지는 독립적으로 유지해야 합니다 (뒤로가기 · 공유 · SEO).

---

## 7. 작업 항목

- [ ] 카드 댓글 입력에 낙관적 업데이트 적용
- [ ] 등록 성공 후 0.5~1초 대기 → `/q/{slug}#c-{id}` 이동
- [ ] 상세 페이지에서 앵커 댓글로 스크롤 + 2초 하이라이트
- [ ] 앵커 대상에 `scroll-margin-top` 지정 (fixed 헤더 높이만큼)
- [ ] 스크롤 오프셋 조정 — 반대 진영 1위 댓글이 함께 보이도록
- [ ] 상세 페이지 "↓ 다음 안건" 가시성 확보
- [ ] 등록 실패 시 이동하지 않고 카드에서 에러 표시 + 입력 내용 보존
- [ ] 낙관적 업데이트 롤백 처리

---

## 8. 유지할 것 (훼손 금지)

- **상세 페이지의 댓글 섹션은 그대로 유지** — 카드 입력은 추가 경로일 뿐,
  검색으로 직접 진입한 사용자는 상세에서 바로 댓글을 쓸 수 있어야 함
- 댓글 진영은 httpOnly 쿠키에서 판정 (클라이언트 값 신뢰 금지)
- 반대편 1위 댓글 상단 고정
- 댓글 최소 길이 5자 · 금칙어 · 신고 3회 자동 숨김
- 투표하지 않으면 댓글 작성 불가

---

## 9. 선행 조건

이 문서의 흐름은 아래가 먼저 반영되어야 성립합니다.

1. **6차 1장 — 결과 공개 임계치 5표로 하향**
   결과가 보이지 않으면 댓글을 쓸 동기 자체가 생기지 않음
2. **7차 3장 — 투표 직후 카드 내 댓글 입력**
   이 문서는 그 다음 단계를 정의한 것

세 문서를 순서대로 반영하세요: **6차 1장 → 7차 3장 → 8차**

---

## 10. 지침 변경 이력 (누적)

| 지침 | 원 출처 | 현재 상태 |
|---|---|---|
| 카드 내부 여백 축소 | 2차 6-3 | ❌ 취소 (4차 1장) |
| 카드 내부 여백 확대 (`space-between`) | 4차 2-1 | ⚠️ 3분할 구조로 대체 (5차 1장) |
| 카드 `min-height: 78dvh` | 4차 2-1 | ⚠️ 68~72dvh (5차 1-3) |
| 퍼센트 공개 임계치 20표 | 1차 1-2 | ⚠️ 5표 (6차 1-2) |
| 판정 카피 임계치 50표 | 1차 1-2 | ⚠️ 20표 (6차 1-2) |
| 참여자 수 100표 게이트 | — | ❌ 폐지, 항상 노출 (6차 1-2) |
| PV 최적화 | 1차 브리프 | ❌ 노출 최적화로 전환 (3차 3장) |
| 다크/라이트 양쪽 지원 | — | ❌ 라이트 고정 (5차 3장) |
| 헤더 `position: sticky` | 3차 5-1 | ⚠️ `fixed` + 컨테이너 밖 (7차 5-3) |
| 댓글은 상세 페이지에만 | 3차 3장 | ⚠️ 카드 입력 추가, 상세는 유지 (7차 3장) |
| 댓글 작성 후 카드에 머무름 | 7차 3장 | ⚠️ **상세 페이지로 자동 이동** (8차) |

---

# 반영 결과 (2026-08-13)

§9 선행 조건은 둘 다 이미 반영된 상태였다(6차 §1 임계치 5표, 7차 §3 카드 내 입력).

## 반영

| 항목 | 반영 |
|---|---|
| §1 올리면 상세로 자동 이동 | `router.push('/q/{slug}#c-{id}')` |
| §3-1 (1) 등록 확인 | 서버가 돌려준 **실제 댓글 본문**을 카드에 표시 |
| §3-1 (2) 0.5~1초 대기 | **0.9초** 뒤 이동 |
| §3-1 (3) `#c-{commentId}` | 댓글 `<li>`에 `id="c-<id>"` |
| §3-1 (4) 착지 + 2초 하이라이트 | `.item:target`에 2초 애니메이션 |
| §3-1 (5) 이탈 경로 | 댓글 섹션 아래에 "↓ 다음 안건으로" 표지 |
| §3-3 헤더 가림 방지 | 이미 `html { scroll-padding-top }`이 처리한다(7차) |
| §3-3 반대편 1위가 함께 보이게 | `.item { scroll-margin-top: 7rem }` — 헤더 여유에 **더해진다** |
| §5 실패 시 이동 안 함 | `if (res.ok)` 안에서만 이동. 실패하면 카드에 머물고 **입력 내용 보존** |
| §8 상세 댓글 섹션 유지 | 그대로 |

## 문서와 다르게 한 판단 2건

### 1. 낙관적 업데이트를 쓰지 않았다 → §7 "롤백 처리"가 필요 없어졌다

§3-1 1단계는 "낙관적 업데이트"를, §7은 그 "롤백 처리"를 요구한다.
**POST 응답이 이미 실제 댓글(id·body·side)을 돌려준다.** 그걸 그대로 그리면
확인된 값이므로 롤백할 상태가 애초에 없다. 목적(§3-2 "등록 확인")은 더 잘 달성된다 —
낙관적 표시는 "등록된 것처럼 보이는" 것이고, 이건 "등록된" 것이다.

### 2. 하이라이트를 JS 타이머가 아니라 `:target`으로 했다

CSS만 쓴다. JS로 클래스를 붙였다 떼면 **뒤로가기·새로고침에서 상태가 어긋나고**,
같은 앵커를 다시 열었을 때 하이라이트가 안 켜진다. `:target`은 URL을 따라가므로 항상 맞는다.
`prefers-reduced-motion`에서는 애니메이션만 끄고 **표시는 남긴다** —
없애면 "어디에 착지했는지"라는 기능 자체가 사라진다.

## 지나가다 잡은 결함 2건

1. 🔴 **상세의 "이어지는 안건"들이 서로 맞붙어 있었다.**
   카드 간격을 `.slide { margin-bottom }`으로 줬는데 상세는 스냅이 꺼져 있어
   `.slide`가 아예 없다(Fragment로 렌더된다). → 간격을 **Feed 래퍼(`.wrap`)의 gap**으로
   옮겨 스냅 여부와 무관하게 한 곳에서 준다.
2. 🔴 **`Comments`의 첫 조회가 6초 뒤였다.** `setInterval`만 걸고 마운트 시 `pull()`이 없었다.
   상세는 ISR 300초 + 목록 60초 캐시라, **사유를 올리고 이 페이지로 넘어오면
   자기 댓글이 없는 화면에 도착**한다. → 마운트 즉시 한 번 받아온다.
3. 상세의 **주 카드에서는 `CardSay`를 렌더하지 않는다.** 그 페이지에는 아래에 진짜 댓글
   섹션의 입력 폼이 있어서 같은 화면에 입력창이 둘이 됐다. 이어지는 피드의 카드들은
   각자 다른 안건이므로 그대로 보여준다.

## 검증

- `tsc --noEmit` · `npm run build`(125페이지) 통과
- 흐름 실측(검증 후 전량 삭제 — `tallies` 0행 · `comments` 0건):
  5표 투입 → 카드에서 사유 제출 **201 `side=b`**(쿠키에서 판정) →
  `/api/comments`에 즉시 반영(`tops.b`가 그 댓글) → 기표 번복 시도 **409**(사유 제출 잠금)
- 렌더: 댓글 앵커 `id="c-N"` 3개 · handoff 표지 있음 · **주 카드에 `CardSay` 없음** ·
  상세 카드 간격은 `.wrap` gap이 담당
- 컴파일된 CSS: `.Comments_item:target { animation: land 2s forwards }` ·
  `@keyframes land` · `.Comments_item { scroll-margin-top: 7rem }` ·
  reduced-motion 래퍼 안의 대체 규칙까지 확인

---

# 후속 (2026-08-13) — 착지 연출 · 다음 안건 버튼 · 미투표 우선 정렬

## 1. 착지가 "툭 점프"였다 → 본문 먼저, 그다음 내려간다

8차 §3-1은 `/q/{slug}#c-{id}`로 보내라고 했고 그대로 했더니 **댓글로 즉시 점프**해서
질문(본문)을 아예 못 보고 지나갔다. 방금 무엇에 답했는지가 화면에서 사라진다.

**URL에 해시를 넣지 않는 쪽으로 바꿨다.** 해시가 있으면 브라우저가 즉시 그 위치로
점프하므로 "위에서 시작"이 성립하지 않는다. 대신 `sessionStorage`로 목표를 넘기고
`components/CommentLanding.tsx`가 연출한다:

    최상단 착지(질문이 보인다) → 0.7초 → 내 댓글까지 smooth 스크롤 → 2.4초 강조

- 쿼리 파라미터를 쓰지 않은 이유: `useSearchParams()`가 필요해지고 그 순간
  **상세 페이지가 ISR을 잃는다**(매 요청 렌더). 세션 저장소는 클라이언트 전용이라 무해하다.
- 서버 HTML에 내 댓글이 없을 수 있으므로(ISR 캐시) 요소가 나타날 때까지
  120ms 간격으로 최대 4초 기다린다. 안 나오면 조용히 포기한다(최상단에 있으면 된다).
- 세션 값을 **즉시 소비**하므로 새로고침으로 연출이 반복되지 않는다.
- 강조는 클래스로 한다 — 해시가 없으니 `:target`이 안 걸리고, 이 연출은 **도착하는 순간**
  켜져야 한다. 직접 `#c-N` 링크로 들어온 경우는 `:target`이 그대로 담당한다.
- 🔴 `.landed`를 전역 클래스로만 두면 특정도가 `.item`(0,1,0)과 같아서
  **로드 순서로 갈린다** — layout.css가 먼저 오므로 모듈이 이기고 강조가 안 보인다.
  → 모듈 안에서 `.item:global(.landed)`(0,2,0)로 묶었다.

## 2. "다음 안건" 버튼 — **카드 오른쪽 아래** (사용자 요청, 한 번 되돌렸다)

처음엔 화면 오른쪽 아래에 **고정 버튼**으로 만들었는데 사용자가 "이상한 곳에 있다"고 했다.
카드와 무관한 자리에 떠 있어서 **무엇의 다음인지 읽히지 않았다.**
→ 고정 버튼(`.nav`)을 통째로 제거하고 **카드의 결재란(`.acts`) 오른쪽 끝**에 붙였다.
카드 안에 있으면 "이 안건 다음"이 자명하다.

- `margin-left: auto`로 오른쪽 끝. 왼쪽에는 공유·상세 링크가 남는다
- `<a href="#slug">`가 아니라 **버튼**이다. 앵커는 클릭마다 히스토리가 쌓여
  카드 20장을 넘기면 뒤로가기를 20번 눌러야 한다. 같은 문서 안 이동이라 URL을 바꿀 이유도 없다.
  `scroll-behavior: smooth`가 있으므로 부드럽게 간다
- 🔴 **`nextSlug`를 몰라도 버튼은 항상 렌더한다.** 홈은 서버에서 1장만 심으므로
  첫 렌더에 다음 슬러그가 없다. 그때 감추면 피드가 도착할 때 버튼이 생겨 `.acts`가
  줄바꿈되고 **카드 높이가 바뀌어 scroll-snap이 흔들린다.**
  자리는 지키고 목표는 누를 때 DOM에서 찾는다(다음 `section.ballot`).
  마지막 카드면 문서 끝으로 보낸다 — 거기 "질문을 다 보셨습니다"가 있다
- 좁은 화면에서 `.acts`가 줄바꿈되면 `margin-left: auto`가 혼자 한 줄을 차지하므로
  그때는 가로로 꽉 채운다

## 3. 미투표 우선 정렬 (7차 §4-3) — 보류를 풀었다

7차에서 **ISR 충돌** 때문에 보류했던 항목이다. 홈이 쿠키를 읽으면 `revalidate = 60`을
잃고 매 요청 렌더가 되는데, 홈은 가장 많이 열리는 페이지다.

**우회 방법**: 홈이 서버에서 심는 카드를 **3장 → 1장**으로 줄이고, 2장부터는
`/api/feed`(이미 `force-dynamic`이라 잃을 캐시가 없다)가 쿠키를 읽어 미투표를 앞으로
정렬해 내려준다. 이미 투표한 카드가 앞에 올 수 있는 경우는 첫 1장뿐이고,
그 카드에는 "처리됨" 도장이 찍혀 "또 나왔네"가 아니라 "내가 처리한 건"으로 읽힌다.

한 화면 = 한 카드이므로 1장만 심어도 첫 화면이 비어 보이지 않고,
sentinel이 600px 앞에서 미리 받아오므로 스크롤도 멈추지 않는다.

- `feedOrder(exclude, votesOf, voted)` — 정렬은 **갈래 안에서만** 한다.
  진지 1 : 병맛 2 교차가 유지되어야 한다(7차 §7 훼손 금지)
- `readVotedIds(request)` — 쿠키는 **기표한 안건에만** 있으므로 이름만 훑는다.
  질문 103개를 순회할 필요가 없다
- 홈은 `excludeId`로 심은 카드를 피드에서 뺀다. 홈(개인화 없음)과 `/api/feed`(개인화)의
  순서가 다르므로 빼지 않으면 같은 안건이 두 번 나온다
- 🔴 **투표한 것을 숨기지 않는다.** 3표일 때 던졌는데 지금 200표면 보러 오고,
  그게 핵심 재방문 이유다. 순서만 뒤로 보낸다

### §4-3(2) "처리됨" 도장

`voted`는 클라이언트 상태라 하이드레이션 후에 켜진다 → **절대 배치**로 흐름에서 뺐다.
흐름에 넣으면 그때 카드 높이가 바뀌고 scroll-snap 위치가 흔들린다.

## 검증

- `tsc` · `build`(125페이지) 통과
- 정렬 실측(**DB 무수정** — 쿠키만 위조해 순서만 확인):
  쿠키 없음 → `wedding-gift-50, video-camera, phone-toilet, gift-only-no-show`가 앞.
  그 4건을 투표한 것으로 표시 → **앞 12개에서 전부 사라짐**.
  교차 패턴 `SMMSMMSMM` 유지 · 중복 0건
- 렌더: 홈 서버 카드 1장 · nav/navNext 홈·상세 각 1 · 투표 전 "처리됨" 0개 ·
  상세 handoff 있음
- 컴파일된 CSS: `.Comments_item__hash.landed`(0,2,0) 확인, 전역 단독 `.landed` 규칙 0개

## 아직 안 한 것 — `/me` 아카이브 (7차 §4-4)

`ottoke:voted`에 **내가 무엇에 어떻게 투표했는지는 이미 다 있다.** 목록 자체는 만들 수 있다.
못 만드는 것은 문서가 요구한 두 항목이다:

| 항목 | 가능? |
|---|---|
| 내가 투표한 안건 목록 + 내 선택 | ✅ localStorage에 있다 |
| 소수파였던 안건 모음 | ✅ 현재 집계를 받아오면 된다 |
| 총 참여 건수 · 소수파 비율 | ✅ 이미 `/me`에 있다 |
| **당시 비율 vs 현재 비율** | ❌ **투표 시점의 비율을 아무 곳에도 저장하지 않는다** |
| **내가 고른 뒤 여론이 뒤집힌 안건** | ❌ 같은 이유 |
| 내가 쓴 한마디 목록 | ❌ `c_<id>` 쿠키는 "썼다"만 알고 본문은 없다 |

🔴 **당시 비율은 기록하지 않기로 했다**(2026-08-13 사용자 결정).
따라서 "당시 vs 현재 비율"과 "여론이 뒤집힌 안건"은 **영구히 불가능한 항목**이다 —
과거든 미래든 근거 데이터가 없고, 없는 값을 지어내면 제1원칙 위반이다.
`/me` 아카이브는 ✅ 항목(투표 목록 · 내 선택 · 현재 비율 · 소수파 모음)까지만 만든다.

---

# 후속 2 (2026-08-13) — 페이지 폭 통일 · `/me` 기표 내역 · 큰 "처리됨" 도장

## 1. 페이지마다 종이 크기가 달랐다 → 홈 기준으로 통일

`body:has(.ballot)`으로 홈·상세만 894px, 목록(`/q`·`/best`·`/c`)과 법적 고지는 720px였다.
"산문은 한 줄이 짧아야 읽기 좋다"는 내 판단이었는데, **페이지를 옮길 때마다 종이 크기가
바뀌는 것**이 더 나쁘다 — 같은 서류철에서 나온 문서로 안 읽힌다.

→ `--stage-w: 894px` **전 페이지 동일**. 대신 산문은 **안쪽에서 글줄만 제한**한다:
`.doc`의 좌우 패딩을 `clamp(1.6rem, 5vw, 4.5rem)`로 키워 글이 가운데로 모인다.
실제 공문서도 종이는 A4지만 본문은 여백을 크게 두고 모인다. 종이 크기와 글줄 길이를
동시에 만족시키는 방법이다.

검증: `/` `/q/[slug]` `/q` `/best` `/terms` `/me` 전부 `--stage-w = 894px`,
`body:has(.ballot)` 실제 규칙 0건(주석만 남음).

## 2. `/me`에 기표 내역 — "내가 투표한 목록은 어디서 보는데?"

**볼 곳이 없었다.** `/me`는 집계 요약만 있었다(총 투표 수 · 다수파 % · 주제별 진행률).
`ottoke:voted`에 무엇에 어떻게 투표했는지 다 있는데 화면에 안 내놓고 있었다.

- 안건별로 **내가 고른 쪽 · 현재 내 비율 · 소수/다수 · 총 표수**를 한 줄씩
- 🔴 판정은 **표본 임계치를 넘긴 것만**(`lib/tiers.ts`). 2표에서 "당신은 소수파"라고 하면
  근거가 없고 그건 숫자를 지어내는 것과 같은 종류의 거짓이다. 미달이면 "개표 중"
- **정렬은 소수파 먼저.** 자기가 소수였던 안건이 이 목록에서 가장 재미있는 지점이고
  다시 보러 올 이유가 된다. 같은 그룹에서는 내 비율이 낮은 순 = 더 외로운 순
- `/me`의 `revalidate` 3600 → **300**. 현재 비율을 붙이므로 1시간 낡은 값은
  "지금 이렇다"는 말과 어긋난다. 이 페이지는 누구에게나 같은 HTML이라(개인화는 전부
  클라이언트) ISR 효율은 그대로다
- 화면에 **"투표 당시 비율은 기록하지 않으므로 그때와 지금을 비교해 드릴 수 없습니다"**를
  명시했다 — 없는 기능을 기다리게 하지 않는다

서버는 여전히 **누가 무엇을 골랐는지 모른다.** 안건과 집계만 내려주고 맞추는 일은
브라우저에서 일어난다. 그래서 로그인이 필요 없다.

## 3. "처리됨" 도장을 기표칸 위에 크게

카드 우상단의 작은 배지였을 때는 눈에 들어오지 않았다.
실제로 처리한 서류는 **내용 위에** 도장을 받는다 — 기표칸이 그 자리다.

`clamp(1.9rem, 7vw, 3.6rem)` · `rotate(-11deg)` · 기표칸 정중앙 · 불투명도 0.26

- ⚠️ **`pointer-events: none`이 필수다.** 이 위를 덮으면 **기표 번복을 못 한다** —
  다른 쪽을 눌러 바꿀 수 있어야 한다(취소는 없지만 번복은 된다)
- ⚠️ 절대 배치여야 한다. `voted`는 하이드레이션 후에 켜지므로 흐름에 넣으면
  그때 카드 높이가 바뀌고 scroll-snap 위치가 흔들린다
- 불투명도를 낮게 유지한다 — 아래 선택지 글자와 「오」 도장이 읽혀야 한다.
  `aria-hidden`이라 스크린리더에는 안 읽히고, 시각적으로도 정보가 아니라 표식이다

검증: 투표 전 `processed` 0개 · `pointer-events: none` 포함 ·
`.choices { position: relative }` 기준점 확인

---

# 후속 3 (2026-08-14) — 홈 첫 카드를 상위권 안에서 매일 회전

## 문제: 1등이 1등이라서 계속 1등이었다

홈 첫 카드는 **표가 가장 많은 진지 질문**이었다(무작위도, 날짜 의존도 없었다).
그런데 첫 카드가 노출을 가장 많이 받으니 표를 가장 많이 얻고, 그래서 계속 첫 카드다.
표가 쌓일수록 잠긴다.

- 재방문자는 미투표 우선 정렬이 풀어 준다(투표하면 내 피드에서 내려간다)
- **신규 방문자는 영원히 같은 첫인상**을 받는다
- 브리프 §3이 원한 시의성("매일 방문할 이유")을 만들 장치가 **없었다**

실측 당시 1위 7표 / 2위 6표로 차이가 1표였다 — 지금은 쉽게 바뀌지만 쌓일수록 고정된다.

## 채택: 상위권 안에서 회전

전체 회전이 아니라 **표 상위 8개 안에서** 돈다. 완전 회전이면 0표짜리가 첫 화면에 올 수 있고
그건 "죽은 사이트" 인상을 준다. 0표는 후보에서 빼고, 후보가 2개 미만이면 회전하지 않는다.

### 🔴 무작위가 아니라 날짜다

`Math.random()`이면 새로고침마다 바뀌어 **ISR 캐시가 무의미해지고** 사용자도
"오늘의 안건"으로 인식하지 못한다. 날짜로 정하면 하루 종일 같은 값이라
`revalidate = 60` 캐시가 그대로 유효하다.

### 🔴 해시가 아니라 순번이다

처음엔 날짜 문자열을 해시해 인덱싱했는데 **충돌 때문에 이틀 연속 같은 안건**이 나왔다
(실측: 21일 중 8/19·8/20이 동일). 그러면 "매일 바뀐다"가 깨진다.
에포크 기준 **날짜 번호를 나머지 연산**하면 후보를 한 바퀴 돌 때까지 겹치지 않는다.

실측(24일): **연속 중복 0회**, 후보 8개가 정확히 3일씩 균등 노출.

### 🔴 KST 기준이다

UTC를 그대로 쓰면 **오전 9시에 안건이 바뀐다.** 한국 사용자에게는 자정에 바뀌어야
"오늘의 안건"이 말이 된다. 서버가 어느 시간대에 있든(Vercel은 UTC) 같은 값이 나오게
UTC+9로 옮겨 날짜만 뗀다.

### ⚠️ 후보는 id 순으로 정렬한 뒤 인덱싱한다

표 순서로 인덱싱하면 하루 중에 표가 움직일 때마다 "오늘의 안건"이 바뀐다 —
오후에 첫 카드가 갈리면 버그로 읽힌다. id 순은 안 흔들린다.

**순서만 정한다. 집계는 건드리지 않는다**(제1원칙).

검증: 2026-08-14(KST) 첫 카드 = `wedding-gift-50`으로 시뮬레이션과 일치.


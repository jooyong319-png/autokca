# 웹 프로젝트 기획 체크리스트

> 새 사이트를 시작할 때 채워 넣고 시작하는 템플릿.
> 오또케(autokca.com) · 딱칼크(ttakcalc.com) · whenstage.com 을 만들며 얻은 교훈을 정리한 것.
> 각 원칙 뒤에 **실제로 뭘 틀렸는지**를 붙여 두었다. 원칙만 있으면 다음에 또 같은 실수를 한다.

---

## 0. 이 문서를 쓰는 방법

1. 새 프로젝트를 시작할 때 이 파일을 복사한다
2. 1~5장의 빈칸을 **코드를 한 줄도 쓰기 전에** 채운다
3. 6장 순서대로 만든다
4. 7장은 만들면서 계속 갱신한다

빈칸을 못 채우겠으면 **아직 만들 때가 아니다.** 그게 이 문서의 목적이다.

---

## 1. 핵심 루프

> 사용자가 반복하는 최소 행동을 **한 문장**으로 쓴다.
> 한 문장으로 못 쓰면 아직 계획이 없는 것이다.

```
핵심 루프:
______ → ______ → ______ → ______
```

**예시 (오또케)**: 질문 읽기 → 투표 → 결과 확인 → 다음 질문

### 왜 중요한가

이 루프가 재미있으면 사이트가 되고, 재미없으면 무엇을 붙여도 안 된다.
그리고 **이후 모든 기능은 "이 루프를 방해하지 않는가"로 판단**하면 된다.

### 실패 사례

오또케에서 투표 결과 공개 임계치를 30표로 잡았다. 표가 4개뿐이라
**투표해도 결과가 안 보였다.** 루프의 3번째 칸이 비어 있었던 것이다.

그런데 원인을 찾기까지 개선 문서를 5개나 쓰며 카드 크기·여백·배경만 만졌다.
**루프를 먼저 점검했으면 첫날 찾았을 문제다.**

### 체크

- [ ] 루프의 각 단계가 실제로 작동하는가 (막힌 칸이 없는가)
- [ ] 한 바퀴 도는 데 몇 초 걸리는가
- [ ] 한 바퀴 돌고 나면 **다시 돌고 싶어지는 이유**가 있는가

---

## 2. 성공 상태 스케치

> **"잘 됐을 때 화면이 어떻게 생겼는가"를 먼저 만든다.**
> 이게 이 문서에서 가장 중요한 항목이다.

```
성공 상태 정의:
- 콘텐츠 수: ______
- 참여/데이터 규모: ______
- 한 화면에 보여야 할 것: ______
```

**예시 (오또케)**: 표 800개 · 댓글 25개 · 6:4로 갈린 상태

### 방법

**더미 데이터로 성공 상태를 먼저 렌더링한다.**
개발용 플래그(`?preview=full` 등)를 만들어 두면 30분이면 된다.

그러면 이런 것들이 즉시 보인다.

- 카드/화면에 어떤 요소가 들어가야 하는가
- 여백이 얼마나 필요한가
- 정보 밀도가 충분한가
- 어떤 요소가 실제로는 불필요한가

**그 다음에** "데이터가 없을 때는 어떻게 보일까"를 거꾸로 설계한다.

### 실패 사례

오또케는 **표 4개인 상태에서 UI를 완성하려 했다.**
표 4개짜리 화면은 무엇을 해도 허전하다. 그런데 계속 카드를 키우고 여백을 조정했다.

성공 상태를 먼저 렌더링했다면 "결과 막대와 댓글이 화면을 채운다"는 걸 알았을 것이고,
빈 상태 대응(임계치·빈 상태 문구)만 설계하면 끝날 문제였다.

### 체크

- [ ] 더미 데이터로 성공 상태를 렌더링해 봤는가
- [ ] 빈 상태 / 중간 상태 / 성공 상태 **3단계**가 각각 설계되어 있는가
- [ ] 빈 상태가 "버그처럼" 보이지 않는가

---

## 3. 가장 위험한 가정

> 프로젝트마다 **"이게 틀리면 나머지가 전부 무의미해지는 가정"** 이 하나 있다.
> 그것을 찾아내고, 가장 싸게 검증할 방법을 정한다.

```
가장 위험한 가정:
______

검증 방법:
______

검증 비용(시간):
______
```

**예시 (오또케)**: "사람들이 한 번 들어와서 10개 이상 연속으로 누를까?"
→ 카톡방에 링크 던지고 20명에게 물어보면 **하루면 안다.**

### 핵심 규칙

**위험한 가정을 검증하는 데 필요한 최소한만 만들고, 나머지는 전부 미룬다.**

### 실패 사례

오또케는 이 검증(사람들에게 실제로 던져 보기)을 하지 않은 채
UI를 다듬는 데 이틀을 썼다. 링크 한 번 돌리는 게 더 빨랐다.

### 체크

- [ ] 가정을 한 문장으로 썼는가
- [ ] 검증에 **1주일 이내**로 도달할 수 있는가
- [ ] 검증 전까지 만들지 **않을** 것 목록이 있는가

---

## 4. 유통 경로

> **"첫 1,000명이 어디서 오는가"에 답하지 못하면 만들지 않는다.**

```
첫 1,000명 유입 경로:
1. ______
2. ______

경로별 소요 기간:
______

그 기간 동안 버틸 방법:
______
```

### 경로별 특성

| 경로 | 소요 기간 | 전제 조건 |
|---|---|---|
| SEO | 최소 6개월 | 백링크 확보 수단이 별도로 있어야 함 |
| 커뮤니티 시딩 | 즉시 | **광고글로 안 보이는 콘텐츠 형태**여야 함 |
| 지인·단톡방 | 즉시 | 규모가 작음. 초기 시드용 |
| 개발자 커뮤니티 (긱뉴스·디스콰이엇) | 즉시 | 사이드프로젝트로서 이야깃거리가 있어야 함 |
| 소셜 공유 | 즉시 | 공유할 **이유**가 콘텐츠에 내장되어야 함 |

### 유통 경로가 기능 우선순위를 결정한다

유통이 시딩·공유라면 **OG 이미지와 캡처용 화면이 1순위 기능**이 된다.
"나중에 붙일 것"이 아니라 MVP 필수 항목이다.

### 실패 사례

**딱칼크**: 계산기 사이트라 커뮤니티에 올리면 광고글이 된다.
유통 경로가 SEO 하나뿐인데 백링크 확보 수단이 없었다.
→ 콘텐츠 품질은 좋은데 **하루 10명**. 만들기 전에 알 수 있었던 문제다.

**오또케**: 다행히 "님들 이거 어떻게 생각함?"이 자연스러운 유일한 콘텐츠 형태라
시딩이 가능했다. 하지만 이건 **운이었지 설계가 아니었다.**

### 체크

- [ ] 첫 1,000명 경로가 구체적인가 ("SEO"는 답이 아니다. "어떤 키워드로 몇 위"까지)
- [ ] 그 경로에 필요한 기능이 MVP에 들어 있는가
- [ ] 경로가 막혔을 때의 대안이 있는가

---

## 5. 콜드스타트 계획

> **대부분의 개인 프로젝트는 여기서 죽는다.**
> 기능은 다 만드는데 "처음에 뭐가 있을 것인가"를 정하지 않는다.

```
초기 콘텐츠: ______개 / 누가 만드나: ______
초기 사용자: ______명 / 어디서 데려오나: ______
콘텐츠 0일 때 화면: ______
콘텐츠 고갈 시 보충 방법: ______
```

### 콜드스타트가 필요한 것을 전부 나열할 것

콘텐츠만이 아니다. **사용자가 만드는 모든 것**이 콜드스타트 대상이다.

- 게시물 / 질문 / 항목 ← 보통 여기까지만 생각한다
- 참여 데이터 (투표 수, 조회 수, 평점)
- UGC (댓글, 리뷰, 답변)
- 소셜 신호 (좋아요, 팔로워)

### 실패 사례

오또케는 **질문 103개를 미리 채운 것은 잘했다.**
그런데 **표와 댓글에 대한 콜드스타트 계획이 없었다.**
질문은 있는데 표가 0이라 화면이 비었다.

### 가짜 데이터로 채우지 말 것

콜드스타트를 시드 데이터나 AI 생성으로 해결하려는 유혹이 생긴다.
**콘텐츠(질문·글)는 괜찮지만, 참여 데이터(투표·평점·댓글)는 안 된다.**

이유:

1. **상품 가치가 사라진다** — "78%가 그렇다"가 재미있는 이유는 진짜이기 때문이다.
   생성된 78%는 정보가 아니다
2. **LLM은 실제 분포를 못 만든다** — 모델은 온건·균형을 선호해서
   실제로 8:2로 갈릴 사안도 5:5 근처로 만든다. 재미의 원천인 "예상 밖의 비율"이 사라진다
3. **데이터가 영구히 오염된다** — 진짜 표가 쌓여도 계속 섞인다. 제거하려면 별도 플래그와 재집계가 필요하고,
   이미 노출된 수치는 되돌릴 수 없다. 이후 확장(예측 게임 등)도 막힌다
4. **애드센스 허위 표시 정책 위반 소지** — 수익이 목표라면 감수할 위험이 아니다
5. **발각 비용이 비대칭** — 커뮤니티는 빠르게 알아챈다. 비율이 부자연스럽게 안정적이거나,
   표는 많은데 댓글이 없거나, 증가 패턴이 이상하면 티가 난다.
   "조작함" 한 줄이 돌면 **주 유입 경로가 통째로 막힌다**

**AI를 써도 되는 영역**: 콘텐츠 생성(발행은 수동 검수), 태그 분류, 카피 작성, 모더레이션 보조.
금지되는 것은 **참여 데이터 생성** 하나다.

**대신 할 것**: 지인·단톡방으로 실제 시드 확보(하루면 가능) + 빈 상태를 잘 디자인하기.

### 원칙 — 자동화의 경계

**생성은 자동, 발행은 수동.**

콘텐츠 파이프라인을 만들 때 자동 발행까지 연결하지 않는다.
품질이 상품인 사이트는 안 좋은 것 하나가 섞이면 바로 티가 난다.
하루 5분 검수로 충분하다.

### 체크

- [ ] 콜드스타트가 필요한 데이터 종류를 **전부** 나열했는가
- [ ] 각각에 대해 "누가, 언제, 어떻게" 채울지 정했는가
- [ ] 빈 상태 화면이 설계되어 있는가

---

## 6. 만드는 순서

```
1. 데이터 모델 + 핵심 루프        ← 작동만. 예쁘지 않아도 됨
2. 성공 상태 렌더링 (더미 데이터)   ← 목표 화면 확인
3. 콜드스타트 (초기 콘텐츠 채우기)
4. 유통 장치 (OG 이미지, 공유, 캡처 화면)
5. 배포 + 시딩                   ← 여기서 처음으로 진짜 데이터를 얻는다
6. UI 다듬기                     ← 이제서야
```

### 6번이 마지막인 이유

**실제 데이터가 없으면 UI 판단이 불가능하다.**

오또케는 5번(시딩) 전에 6번(UI 다듬기)을 **다섯 번** 했다.
표가 4개인 상태에서 내린 UI 판단은 표가 800개가 되면 대부분 다시 뒤집힌다.

실제로 지침이 두 번 뒤집혔다.

| 지침 | 1차 판단 | 이후 |
|---|---|---|
| 카드 내부 여백 | 축소 | 확대 → 3분할 구조 |
| 최적화 대상 | PV(페이지뷰) | 노출(impression) — 무한 스크롤이라 PV가 안 늘어남 |

**데이터를 얻기 전의 UI 작업은 대부분 재작업된다.**

---

## 7. 문서 구성

프로젝트당 아래 4종을 둔다.

| 문서 | 내용 | 작성 시점 |
|---|---|---|
| **브리프** | 핵심 루프 / 설계 원칙 / MVP 범위 / **안 할 것 목록** | 시작 전 |
| **성공 상태 스케치** | 잘 됐을 때의 화면. 더미 렌더링 스크린샷 포함 | 시작 전 |
| **콜드스타트 & 유통 플랜** | 초기 콘텐츠·사용자, 첫 1,000명 경로 | 시작 전 |
| **개선 문서** | 실측 기반 수정 | **실제 데이터가 생긴 후** |

### "안 할 것 목록"이 가장 중요하다

없으면 기능이 계속 늘어난다.
재미있어 보이지만 지금 하면 안 되는 것들을 **명시적으로 적어 둔다.**

오또케의 예: 예측 게임, 성향 프로필, 친구 비교, 회원 기능, 타 사이트 연결.

### 개선 문서를 여러 번 쓸 때

- 매 문서에 **"반영 확인 / 미반영"** 목록을 넣는다
- **"유지할 것 (훼손 금지)"** 목록을 넣는다 — 안 그러면 잘 된 부분까지 갈아엎는다
- **"지침 변경 이력"** 표를 넣는다 — 구조가 바뀌면 이전 지침이 뒤집힌다.
  어느 것이 최신인지 명시하지 않으면 자동화 도구가 옛 지침을 따라간다

---

## 8. README에 넣을 것

코드 저장소 README는 "설치 방법"만 적는 곳이 아니다.
**미래의 자신(그리고 코딩 에이전트)을 위한 문서**로 쓴다.

- [ ] **기준 문서 선언** — "판단이 갈리면 `___.md`의 원칙으로 결정한다"
- [ ] **설계 원칙 ↔ 코드 위치 매핑 표** — 왜 이렇게 짰는지 잊지 않기 위해
- [ ] **손대기 전에 알아야 할 것** — 바꾸면 안 되는 값(ID, slug 등)과 그 이유
- [ ] **알려진 함정** — 실제로 겪은 삽질. 날짜와 함께 기록
- [ ] **배포 전 체크리스트** — 환경변수, 도메인 정본 설정, 검증 스크립트
- [ ] **운영 항목** — 콘텐츠 큐 관리, 임계치, 모니터링

### 원칙은 코드로 강제한다

문서에만 있는 원칙은 지켜지지 않는다.

오또케 예: "가짜 숫자를 채우지 않는다"를 문서에만 두지 않고,
**집계 서버 미연결 시 결과를 감추고 그 사실을 화면에 밝히도록** 구현했다.
이러면 원칙을 어기려면 코드를 고쳐야 하므로 실수로 어길 수 없다.

---

## 9. 자주 하는 실수 요약

| 실수 | 증상 | 대응 |
|---|---|---|
| 빈 상태에서 UI 완성하려 함 | 아무리 고쳐도 허전함 | 성공 상태를 먼저 렌더링 (2장) |
| 유통 경로 없이 만듦 | 잘 만들었는데 방문자 없음 | 첫 1,000명 경로를 먼저 정의 (4장) |
| 콜드스타트 미계획 | 배포했는데 화면이 빔 | 콘텐츠·참여 데이터 각각 계획 (5장) |
| 게이트·임계치 과설정 | 콘텐츠가 전부 가려짐 | UX용·SEO용 임계치를 분리 |
| 데이터 전에 UI 다듬기 | 나중에 전부 재작업 | 시딩 후로 미룸 (6장) |
| 지침 충돌 관리 안 함 | 자동화 도구가 옛 지침 따라감 | 변경 이력표 유지 (7장) |
| 기능 무한 증식 | 출시가 계속 밀림 | "안 할 것 목록" (7장) |

---

## 10. 최소 실행 체크리스트

새 프로젝트 시작 시 **이것만은** 반드시.

- [ ] 핵심 루프를 한 문장으로 썼다
- [ ] 성공 상태를 더미 데이터로 렌더링해 봤다
- [ ] 가장 위험한 가정과 검증 방법을 정했다
- [ ] 첫 1,000명이 어디서 오는지 구체적으로 답할 수 있다
- [ ] 초기 콘텐츠를 누가 언제 채울지 정했다
- [ ] "안 할 것 목록"을 썼다
- [ ] 유통 경로에 필요한 기능이 MVP에 들어 있다
